desc: AP1015-50 進捗取得_一括取得
runners:
  req:
    endpoint: ${BACKEND_URL}
    openapi3: ${OPENAPI3_URL}
    skipValidateRequest: false
    skipValidateResponse: false
vars:
  placeScheduleId: "abcd1015-0000-0000-0000-000000000001"
steps:
  getAccessToken:
    desc: "アクセストークンを取得します"
    include:
      path: ../../Helpers/adminAccessToken.yml
  get:
    desc: "進捗取得_一括取得"
    req:
      "/api/v1/progress/{{ vars.placeScheduleId }}":
        get:
          headers:
            Authorization: "Bearer {{ steps.getAccessToken.token }}"
    test: |
      current.res.status == 200
      && len(current.res.body.progress) == 1
      // 予定
      && current.res.body.progress[0].details[0].count ==  19
      // 来場
      && current.res.body.progress[0].details[1].count ==  19
      // 済
      && current.res.body.progress[0].details[2].count ==  7
      // 中止
      && current.res.body.progress[0].details[3].count ==  1
    dump:
      expr: current.res.body
      out: ../Dump/AP1015-50.json