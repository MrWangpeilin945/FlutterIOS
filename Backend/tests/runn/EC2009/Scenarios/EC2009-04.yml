desc: EC2009-04 検査項目明細IDが取得できない
runners:
  db: ${DB_HOST}
  req:
    endpoint: ${BACKEND_URL}
    skipValidateRequest: false
    skipValidateResponse: false
vars:
  request: "json://../Requests/EC2009-04.json"
steps:
  clearTable:
    desc: "検査基準値範囲テーブルのクリア"
    db:
      query: |
        -- 基準値パターン
        TRUNCATE TABLE resultcollector.thresholds CASCADE;
        -- 検査基準値範囲
        TRUNCATE TABLE resultcollector.exam_normal_value_range CASCADE;
        -- 外部検査項目明細
        TRUNCATE TABLE resultcollector.external_exam_item_details CASCADE;
        -- 検査メニュー
        TRUNCATE TABLE resultcollector.exam_menus CASCADE;
  insertExamMenus:
    desc: "関連テーブルに実行前データを登録"
    db:
      query: |
        -- 基準値パターン
        INSERT INTO
          resultcollector.thresholds
          (threshold_id, threshold_code, name, order_number, created_at, created_by)
        VALUES('e0002009-0000-0000-0000-000000000001', 'EC2009', 'EC2009血液検査', 1, CURRENT_TIMESTAMP,'ec2009');
  post:
    desc: "検査項目明細IDが取得できない"
    req:
      /api/v1/external/examNormalValueRange:
        post:
          body:
            application/json:
              "{{ vars.request }}"
    test: |
      current.res.status == 207
      && len(current.res.body) == 1
      // 検査項目明細IDエラーがErrorObjectで返される
      && current.res.body[0].code == "10001"
      && current.res.body[0].inputNote == "2009041"
      && current.res.body[0].message == "指定されたExamItemDetailCdがシステム上に存在しません。Code:Dummy2009WBC"
    dump:
      expr: current.res.body
      out: ../Dump/EC2009-04.json
  confirmExamNormalValueRange:
    desc: "検査基準値範囲の登録を確認"
    db:
      query: |
        SELECT
          range_id::text
        FROM
          resultcollector.exam_normal_value_range
    test: |
      // 検査基準値範囲が登録されていないことを確認
      len(current.rows) == 0
