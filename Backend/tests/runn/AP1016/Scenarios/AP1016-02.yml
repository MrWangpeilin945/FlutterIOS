desc: AP1016-02 会場ロック取得_指定した会場日程IDの形式が不正 [400]
runners:
  req:
    endpoint: ${BACKEND_URL}
    openapi3: ${OPENAPI3_URL}
    skipValidateRequest: false
    skipValidateResponse: true
vars:
  placeScheduleId: "aceced00-0000-0000-0000-000000000001invalid"
steps:
  getAccessToken:
    desc: "アクセストークンを取得します"
    include:
      path:
        ../../Helpers/adminAccessToken.yml
  get:
    desc: "会場ロック取得_指定した会場日程IDの形式が不正"
    req:
      /api/v1/placeSchedules/{{ vars.placeScheduleId }}/placeScheduleLockingStatus:
        get:
          headers:
            Authorization: "Bearer {{ steps.getAccessToken.token }}"
    test: |
      current.res.status == 400
    dump:
      expr: current.res.body
      out: ../Dump/AP1016-02.json

