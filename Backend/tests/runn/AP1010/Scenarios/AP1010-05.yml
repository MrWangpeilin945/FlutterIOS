desc: AP1010-01 検査内容取得_同姓同名チェック [あり]
runners:
  req:
    endpoint: ${BACKEND_URL}
    openapi3: ${OPENAPI3_URL}
    skipValidateRequest: false
    skipValidateResponse: false
vars:
  consultNumber: "10101"
  examMenuId: 1
  response: "json://../Dump/AP1010-05.json"
steps:
  getAccessToken:
    desc: "アクセストークンを取得します"
    include:
      path:
        ../../Helpers/adminAccessToken.yml
  get:
    desc: "検査内容取得_正常"
    req:
      /api/v1/consult/{{ vars.consultNumber }}/examItems?examMenuId={{ vars.examMenuId }}:
        get:
          headers:
            Authorization: "Bearer {{ steps.getAccessToken.token }}"
    test: |
      current.res.status == 200
      // 同姓同名あり
      && vars.response.examinee.sameNameAlert == true
    dump:
      expr: current.res.body
      out: ../Dump/AP1010-05.json

