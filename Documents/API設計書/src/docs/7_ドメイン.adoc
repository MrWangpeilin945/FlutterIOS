= ドメイン

ドメイン層の設計を示します。

値オブジェクトやエンティティなどは厳密に区別しません。できるだけオブジェクトに業務ロジックを凝集させることが目的です。

* 年齢
* 受診者
* 会場日程
* 受診
* 中止理由
* 検査機器
* ホームメニュー

== 列挙型

列挙型（Enum）で表現するものを一覧で整理します。

ここに記載しているものはコード定義書.xlsxをインプットにしています。
コード定義書が未確定のため、あくまでもバックエンド実装のための参考情報です。（2024-11-14現在）

=== 性別

受診者の生物学的性別を表現するコード。国際規格であるISO 5218に則る。

* https://ja.wikipedia.org/wiki/ISO_5218[ISO 5218 - Wikipedia]
* https://qiita.com/aoshirobo/items/32deb45cb8c8b87d65a4[システムで「性別」の情報を扱う前に知っておくべきこと - Qiita]

----
enum Sex
{
    不明 = 0,
    男 = 1,
    女 = 2,
    適用不能 = 9
}
----

=== 対象性別

基準値などの設定に使用します。

[source,csharp]
----
enum TargetSex
{
    男 = 1,
    女 = 2,
    両方 = 3
}
----

=== 受診進捗状況

受診単位の進捗ステータスです。

[source,csharp]
----
enum ConsultProgressStatus
{
    来場待ち = 11,
    検査中 = 21,
    キャンセル = 41
}
----

=== 検査進捗状況

検査項目単位の進捗ステータスです。

[source,csharp]
----
enum ExamItemProgressStatus
{
    未実施 = 11,
    検査済み = 41,
    検査中止 = 51
}
----

=== 集計検査進捗状況

進捗画面で取り扱うステータスです。

[source,csharp]
----
enum AggregatedProgressStatus
{
    予定 = 11,
    来場 = 21,
    済 = 41,
    中止 = 51
}
----

=== 会場日程状況

会場ロックのステータスです。 

[source,csharp]
----
enum PlaceScheduleLockingStatus
{
    検査中 = 21,
    検査完了 = 31
}
----

=== 会場日程データ出力状況

会場日程単位のデータ出力ステータスです。 

[source,csharp]
----
enum PlaceScheduleResultExportStatus
{
    未出力 = 11,
    出力済み = 31,
    出力エラー = 41
}
----

=== 受診データ出力状況

受診単位のデータ出力ステータスです。

[source,csharp]
----
enum ConsultResultExportStatus
{
    未出力 = 11,
    出力保留 = 21,
    出力済み = 31
}
----

=== キーボード種別

ソフトウェアキーボードのタイプです。

[source,csharp]
----
enum KeyboardType
{
    テンキー = 1
}
----

=== 検査項目グループ種別

コンポーネントを出し分けるためのタイプです。

[source,csharp]
----
enum ExamItemGroupType
{
    単一数値,
    単一選択,
    尿検査,
    インクリメント,
    左右数値,
    左右選択,
    身体計測,
    血圧1,
    血圧2,
    血圧3,
    視力,
    聴力,
    通過
}
----

=== 入力エラーレベル

入力した結果値に対するエラーを表示するときのレベルです。

[source,csharp]
----
enum InputErrorLevel
{
    正常 = 1,
    警告 = 2,
    異常 = 3
}
----
