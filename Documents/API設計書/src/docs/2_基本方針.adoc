= 基本方針

* パスとプロパティはキャメルケース（camelCase）
* JSON形式
* RESTfulを基本とする
* 日付はRFC3339形式とする
* ログインAPI以外は全て認証用のアクセストークンで保護する
* `http(s)://{ホスト名}/api/v1` を共通部分とする

== バージョニング

APIに破壊的変更を加える際、新旧バージョンを並行で稼働させる必要があることを見据えてパスによるバージョニングを導入する。
初期開発においては `v1` を固定で利用する。
