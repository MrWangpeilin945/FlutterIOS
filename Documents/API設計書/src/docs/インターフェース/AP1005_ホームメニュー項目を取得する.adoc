== AP1005_ホームメニュー項目を取得する

=== 概要

* ホーム画面の表示に必要なホームメニューを取得する
* ホームメニューの項目は設定したグループでグルーピングする
* グループとグループ内の項目は表示順の昇順でソートする
* メニューごとに使用可能条件を設定する。すべての条件を満たすメニューを利用できる。ただし空リストの場合は制限なく使用できる。
** `placeScheduleSelected`: 会場日程が選択済みである
** `placeScheduleUnlocked`: 会場ロックが解除されている

=== URI

[source]
----
[GET] /homeMenus
----

=== リクエストヘッダー

=== パスパラメータ

なし

=== クエリパラメータ

なし

=== リクエストボディ

なし

=== レスポンスボディ

[source,json]
----
{
  "homeMenuGroups": [
    {
      "groupName": "一般",
      "menus": [
        {
          "menuName": "進捗",
          "path": "progress",
          "availableConditions": [
            "placeScheduleSelected"
          ]
        }
      ]
    },
    {
      "groupName": "外部システム連携",
      "menus": [
        {
          "menuName": "健診結果出力",
          "path": "examResultsExport",
          "availableConditions": []
        },
        {
          "menuName": "健診結果出力履歴",
          "path": "examResultsExportHistory",
          "availableConditions": []
        }
      ]
    }
  ],
  "placeScheduleLockingStatus" : 1
}
----

=== ステータスコード

* 200 OK
* 401 Unauthorized
** JWT認証に失敗
