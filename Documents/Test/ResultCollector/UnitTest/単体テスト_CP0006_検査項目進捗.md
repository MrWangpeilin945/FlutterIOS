# CP0006_検査項目進捗

## 初期表示

### 検査項目名

#### 正常 未実施 　
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のexamItemNameが表示されていること

### 状態名称
#### 正常 未実施 左から1番目のボタン（状態：済）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=41に紐づくstatusNameが表示されていること

#### 正常 未実施 左から2番目のボタン（状態：中止）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=51に紐づくstatusNameが表示されていること

#### 正常 未実施 左から3番目のボタン（状態：来場）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=21に紐づくstatusNameが表示されていること

#### 正常 未実施 左から4番目のボタン（状態：予定）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=11に紐づくstatusNameが表示されていること

#### 正常 未実施 左から5番目のボタン（状態：その他）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=71に紐づくstatusNameが表示されていること

### カウント
#### 正常 未実施 左から1番目のカウント（状態：済）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=41に紐づくcountが表示されていること

#### 正常 未実施 左から2番目のカウント（状態：中止）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=51に紐づくcountが表示されていること

#### 正常 未実施 左から3番目のカウント（状態：来場）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=21に紐づくcountが表示されていること

#### 正常 未実施 左から4番目のカウント（状態：予定）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=11に紐づくcountが表示されていること

#### 正常 未実施 左から5番目のカウント（状態：その他）
1. 検査項目進捗コンポーネントが呼び出される
* [ ] 引数で渡された進捗情報のdetails.status=71に紐づくcountが表示されていること

### 進捗表示
#### 正常 未実施 　
1. 検査項目進捗コンポーネントが呼び出される
* [ ] カウント情報から割合が表示されていること

## 状態名称ボタン押下

### 左から1番目のボタン（状態：済）

#### 正常 未実施 　
1. 検査項目進捗コンポーネントが呼び出される
2. 左から1番目のボタンを押下する
* [ ] 何も処理されないこと

### 左から2番目のボタン（状態：中止）

#### 正常 未実施 　
1. 検査項目進捗コンポーネントが呼び出される
2. 左から2番目のボタンを押下する
* [ ] 何も処理されないこと

### 左から3番目のボタン（状態：来場）

#### 正常 未実施 　
1. 検査項目進捗コンポーネントが呼び出される
2. 左から3番目のボタンを押下する
* [ ] 何も処理されないこと

### 左から4番目のボタン（状態：予定）

#### 正常 未実施 　
1. 検査項目進捗コンポーネントが呼び出される
2. 左から4番目のボタンを押下する
* [ ] 何も処理されないこと

### 左から5番目のボタン（状態：その他）

#### 正常 未実施 　
1. 検査項目進捗コンポーネントが呼び出される
2. 左から5番目のボタンを押下する
* [ ] 何も処理されないこと