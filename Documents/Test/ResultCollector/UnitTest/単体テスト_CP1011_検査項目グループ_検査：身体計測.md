# CP1011_検査項目グループ_検査：身体計測

## 初期表示　

### データ確認

#### 異常 未実施 引数.検査項目情報の examItems が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：身体計測コンポーネントが表示されていないこと

#### 異常 未実施 引数.検査項目情報の examItems に要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：身体計測コンポーネントが表示されていないこと

#### 異常 未実施 引数.検査項目情報の examItems に positionNumber が「1」「2」「3」の要素が全て存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：身体計測コンポーネントが表示されていないこと

#### 異常 未実施 引数.検査項目情報の examItems に positionNumber が「1」「2」「3」の要素内の各examItemDetails.positionNumberが「1」の要素が全て存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：身体計測コンポーネントが表示されていないこと

#### 正常 未実施 引数.検査項目情報の examItems の positionNumber が「1」の要素の examItemDetails の中に positionNumber が「1」の要素が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：身体計測コンポーネントが表示されること

#### 正常 未実施 引数.検査項目情報の examItems の positionNumber が「2」の要素の examItemDetails の中に positionNumber が「1」の要素が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：身体計測コンポーネントが表示されること

#### 正常 未実施 引数.検査項目情報の examItems の positionNumber が「3」の要素の examItemDetails の中に positionNumber が「1」の要素が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：身体計測コンポーネントが表示されること

### 項目名（身長）

#### 異常 未実施 examItems.positionNumber が「1」のnameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「"身長"」が表示されていること

#### 正常 未実施 examItems.positionNumber が「1」のnameが存在する時/nameが8文字以下の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examItems.positionNumberが「1」のnameが表示されること

#### 正常 未実施 examItems.positionNumber が「1」のnameが存在する時/nameが9文字以上の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examItems.positionNumberが「1」のnameが先頭から8文字表示されること

### 入力値（身長）

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の value が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{value}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の value が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（身長）が空欄で表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のhasOrderがfalseの時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（身長）がグレーアウトで表示されていること
* [ ] 入力値（身長）に入力できないこと

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のcancelReasonIdが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（身長）がグレーアウトで表示されていること
* [ ] 入力値（身長）に入力できないこと

#### 異常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（身長）がグレーアウトで表示されていること
* [ ] 入力値（身長）に入力できないこと

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のexamRegistResults に含まれる errorLevel の最大値が 3 の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（身長）がテーマの"error"の色で表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のexamRegistResults に含まれる errorLevel の最大値が 2 の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（身長）がテーマの"warning"の色で表示されること

### 前回値（身長）

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のprevValue が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{prevValue}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のprevValueが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 前回値(身長)が表示されていないこと

#### 異常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 前回値(身長)が表示されていないこと

### 単位（身長）

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の unit が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{unit}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の unit が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 単位(身長)が表示されていないこと

#### 異常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 単位(身長)が表示されていないこと

### クリアボタン（身長）

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のhasOrderがtrueの時 かつ cancelReasonIdが存在しない時 　
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（身長）が表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のhasOrderがfalseの時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（身長）がグレーアウトで表示されていること
* [ ] クリアボタン（身長）が押下できないこと

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のcancelReasonIdが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（身長）がグレーアウトで表示されていること
* [ ] クリアボタン（身長）が押下できないこと

#### 異常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（身長）がグレーアウトで表示されていること
* [ ] クリアボタン（身長）が押下できないこと

### エラーメッセージ（身長）

#### 正常 未実施 examItems.positionNumber が「1」のexamRegistResultsが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていないこと

#### 正常 未実施 examItems.positionNumber が「1」のexamRegistResultsが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examRegistResults の要素数分表示されていること
* [ ] examRegistResults の各要素の errorLevel の降順で表示されていること

#### 正常 未実施 examItems.positionNumber が「1」のexamRegistResultsに errorLevel が 3 の値が設定されている時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「1」のexamRegistResultsに errorLevel が 2 の値が設定されている時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"warning"で表示されていること

#### 正常 未実施 onRegisterPressed が false の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「{項目名（身長）}は入力必須項目です。」、「{項目名（身長）}は半角数字で入力してください。」、「入力に誤りがあります。」、「入力値を確認してください。」が表示されないこと

### 項目名（体重）

#### 異常 未実施 examItems.positionNumber が「2」のnameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「"体重"」が表示されていること

#### 正常 未実施 examItems.positionNumber が「2」のnameが存在する時/nameが8文字以下の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examItems.positionNumberが「2」のnameが表示されること

#### 正常 未実施 examItems.positionNumber が「2」のnameが存在する時/nameが9文字以上の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examItems.positionNumberが「2」のnameが先頭から8文字表示されること

### 入力値（体重）

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の value が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{value}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の value が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体重）が空欄で表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のhasOrderがfalseの時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体重）がグレーアウトで表示されていること
* [ ] 入力値（体重）に入力できないこと

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のcancelReasonIdが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体重）がグレーアウトで表示されていること
* [ ] 入力値（体重）に入力できないこと

#### 異常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体重）がグレーアウトで表示されていること
* [ ] 入力値（体重）に入力できないこと

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のexamRegistResults に含まれる errorLevel の最大値が 3 の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体重）がテーマの"error"の色で表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のexamRegistResults に含まれる errorLevel の最大値が 2 の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体重）がテーマの"warning"の色で表示されること

### 前回値（体重）

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のprevValue が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{prevValue}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のprevValueが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 前回値(体重)が表示されていないこと

#### 異常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 前回値(体重)が表示されていないこと

### 単位（体重）

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の unit が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{unit}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の unit が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 単位(体重)が表示されていないこと

#### 異常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 単位(体重)が表示されていないこと

### クリアボタン（体重）

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のhasOrderがtrueの時 かつ cancelReasonIdが存在しない時 　
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（体重）が表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のhasOrderがfalseの時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（体重）がグレーアウトで表示されていること
* [ ] クリアボタン（体重）が押下できないこと

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のcancelReasonIdが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（体重）がグレーアウトで表示されていること
* [ ] クリアボタン（体重）が押下できないこと

#### 異常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（体重）がグレーアウトで表示されていること
* [ ] クリアボタン（体重）が押下できないこと

### エラーメッセージ（体重）

#### 正常 未実施 examItems.positionNumber が「2」のexamRegistResultsが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていないこと

#### 正常 未実施 examItems.positionNumber が「2」のexamRegistResultsが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examRegistResults の要素数分表示されていること
* [ ] examRegistResults の各要素の errorLevel の降順で表示されていること

#### 正常 未実施 examItems.positionNumber が「2」のexamRegistResultsに errorLevel が 3 の値が設定されている時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「2」のexamRegistResultsに errorLevel が 2 の値が設定されている時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"warning"で表示されていること

#### 正常 未実施 onRegisterPressed が false の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「{項目名（体重）}は入力必須項目です。」、「{項目名（体重）}は半角数字で入力してください。」、「入力に誤りがあります。」、「入力値を確認してください。」が表示されないこと

### 項目名（体脂肪率）

#### 異常 未実施 examItems.positionNumber が「3」のnameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「"体脂肪率"」が表示されていること

#### 正常 未実施 examItems.positionNumber が「3」のnameが存在する時/nameが8文字以下の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examItems.positionNumberが「3」のnameが表示されること

#### 正常 未実施 examItems.positionNumber が「3」のnameが存在する時/nameが9文字以上の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examItems.positionNumberが「3」のnameが先頭から8文字表示されること

### 入力値（体脂肪率）

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の value が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{value}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の value が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体脂肪率）が空欄で表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のhasOrderがfalseの時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体脂肪率）がグレーアウトで表示されていること
* [ ] 入力値（体脂肪率）に入力できないこと

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のcancelReasonIdが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体脂肪率）がグレーアウトで表示されていること
* [ ] 入力値（体脂肪率）に入力できないこと

#### 異常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体脂肪率）がグレーアウトで表示されていること
* [ ] 入力値（体脂肪率）に入力できないこと

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のexamRegistResults に含まれる errorLevel の最大値が 3 の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体脂肪率）がテーマの"error"の色で表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のexamRegistResults に含まれる errorLevel の最大値が 2 の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 入力値（体脂肪率）がテーマの"warning"の色で表示されること

### 前回値（体脂肪率）

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のprevValue が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{prevValue}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のprevValueが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 前回値(体脂肪率)が表示されていないこと

#### 異常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 前回値(体脂肪率)が表示されていないこと

### 単位（体脂肪率）

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の unit が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{unit}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の unit が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 単位(体脂肪率)が表示されていないこと

#### 異常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 単位(体脂肪率)が表示されていないこと

### クリアボタン（体脂肪率）

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のhasOrderがtrueの時 かつ cancelReasonIdが存在しない時 　
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（体脂肪率）が表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のhasOrderがfalseの時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（体脂肪率）がグレーアウトで表示されていること
* [ ] クリアボタン（体脂肪率）が押下できないこと

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のcancelReasonIdが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（体脂肪率）がグレーアウトで表示されていること
* [ ] クリアボタン（体脂肪率）が押下できないこと

#### 異常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] クリアボタン（体脂肪率）がグレーアウトで表示されていること
* [ ] クリアボタン（体脂肪率）が押下できないこと

### エラーメッセージ（体脂肪率）

#### 正常 未実施 examItems.positionNumber が「3」のexamRegistResultsが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていないこと

#### 正常 未実施 examItems.positionNumber が「3」のexamRegistResultsが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examRegistResults の要素数分表示されていること
* [ ] examRegistResults の各要素の errorLevel の降順で表示されていること

#### 正常 未実施 examItems.positionNumber が「3」のexamRegistResultsに errorLevel が 3 の値が設定されている時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「3」のexamRegistResultsに errorLevel が 2 の値が設定されている時
3. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"warning"で表示されていること

#### 正常 未実施 onRegisterPressed が false の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「{項目名（体脂肪率）}は入力必須項目です。」、「{項目名（体脂肪率）}は半角数字で入力してください。」、「入力に誤りがあります。」、「入力値を確認してください。」が表示されないこと

### 項目名（BMI）

#### 異常 未実施 examItems.positionNumber が「4」のnameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「"BMI"」が表示されていること

#### 正常 未実施 examItems.positionNumber が「4」のnameが存在する時/nameが8文字以下の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examItems.positionNumberが「4」のnameが表示されること

#### 正常 未実施 examItems.positionNumber が「4」のnameが存在する時/nameが9文字以上の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examItems.positionNumberが「4」のnameが先頭から8文字表示されること

### 計算値（BMI）

#### 正常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」の value が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{value}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」の value が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 計算値（BMI）が空欄で表示されていること

#### 正常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のhasOrderがfalseの時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 計算値（BMI）がグレーアウトで表示されていること

#### 正常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のcancelReasonIdが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 計算値（BMI）がグレーアウトで表示されていること

#### 異常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 計算値（BMI）がグレーアウトで表示されていること

#### 正常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のexamRegistResults に含まれる errorLevel の最大値が 3 の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 計算値（BMI）がテーマの"error"の色で表示されていること

#### 正常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のexamRegistResults に含まれる errorLevel の最大値が 2 の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 計算値（BMI）がテーマの"warning"の色で表示されること

### 前回値（BMI）

#### 正常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のprevValue が存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 「{prevValue}」が表示されていること

#### 正常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のprevValueが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 前回値（BMI）が表示されていないこと

#### 異常 未実施 examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」の要素が存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] 前回値（BMI）が表示されていないこと

### エラーメッセージ（BMI）

#### 正常 未実施 examItems.positionNumber が「4」のexamRegistResultsが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていないこと

#### 正常 未実施 examItems.positionNumber が「4」のexamRegistResultsが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] examRegistResults の要素数分表示されていること
* [ ] examRegistResults の各要素の errorLevel の降順で表示されていること

#### 正常 未実施 examItems.positionNumber が「4」のexamRegistResultsに errorLevel が 3 の値が設定されている時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「4」のexamRegistResultsに errorLevel が 2 の値が設定されている時
3. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"warning"で表示されていること

## 入力値（身長）押下

### 入力値（身長）

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「1」の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
* [ ] キーボード(整数)が表示されること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「1」の時/2回押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(整数)が表示される。
4. 入力値（身長）を押下する。
* [ ] キーボード(整数)が表示されること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「2」の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
* [ ] キーボード(選択)が表示されること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「2」の時/2回押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(選択)が表示される。
4. 入力値（身長）を押下する。
* [ ] キーボード(選択)が表示されること

#### 異常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「2」のkeyboard.keyboardTypeが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
* [ ] キーボード(整数)が表示されること

### キーボード(整数)

#### 正常 未実施 キーボード(整数)で数字ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
* [ ] キーボード(整数)から渡された値が入力値（身長）に表示されていること

#### 正常 未実施 integerLength、decimalLength が設定されているときにキーボード(整数)で数字ボタンを押下した時
1. value を「"1234"」、integerLength を「3」、decimalLength を「1」に設定する
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. 入力値（身長）を押下する。
4. キーボード(整数)が表示される。
5. 数字ボタンを押下する。
* [ ] キーボード(整数)から渡された「"123.4"」が入力値（身長）に表示されていること

#### 正常 未実施 integerLength と decimaLength が設定されており decimaLength が 0 の時に数字ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
* [ ] キーボード(整数)から渡された値が入力値（身長）に表示されていること
* [ ] キーボード(整数)から渡された値に小数点が含まれていないこと

#### 正常 未実施 キーボード(整数)で AC ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(整数)が表示される。
4. AC ボタンを押下する。
* [ ] 入力値（身長）の値がクリアされていること

#### 正常 未実施 キーボード(整数)で確定ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(整数)が表示される。
4. 確定ボタンを押下する。
* [ ] キーボード(整数)が表示されていないこと

#### 正常 未実施 キーボード(整数)以外を押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(整数)が表示される。
4. キーボード(整数)以外を押下する。
* [ ] キーボード(整数)が表示されていないこと

### キーボード(選択)

#### 正常 未実施 キーボード(選択)でカスタムボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(選択)が表示される。
4. カスタムボタンを押下する。
* [ ] キーボード(選択)から渡された値が入力値（身長）に表示されていること

#### 正常 未実施 キーボード(選択)で AC ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(選択)が表示される。
4. AC ボタンを押下する。
* [ ] 入力値（身長）の値がクリアされていること

#### 正常 未実施 キーボード(選択)以外を押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(選択)が表示される。
4. キーボード(選択)以外を押下する。
* [ ] キーボード(選択)が表示されていないこと

## 入力値（体重）押下

### 入力値（体重）

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「1」の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
* [ ] キーボード(整数)が表示されること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「1」の時/2回押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(整数)が表示される。
4. 入力値（体重）を押下する。
* [ ] キーボード(整数)が表示されること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「2」の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
* [ ] キーボード(選択)が表示されること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「2」の時/2回押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(選択)が表示される。
4. 入力値（体重）を押下する。
* [ ] キーボード(選択)が表示されること

#### 異常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「2」のkeyboard.keyboardTypeが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
* [ ] キーボード(整数)が表示されること

### キーボード(整数)

#### 正常 未実施 キーボード(整数)で数字ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
* [ ] キーボード(整数)から渡された値が入力値（体重）に表示されていること

#### 正常 未実施 integerLength、decimalLength が設定されているときにキーボード(整数)で数字ボタンを押下した時
1. value を「"1234"」、integerLength を「3」、decimalLength を「1」に設定する
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. 入力値（体重）を押下する。
4. キーボード(整数)が表示される。
5. 数字ボタンを押下する。
* [ ] キーボード(整数)から渡された「"123.4"」が入力値（体重）に表示されていること

#### 正常 未実施 integerLength と decimaLength が設定されており decimaLength が 0 の時に数字ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
* [ ] キーボード(整数)から渡された値が入力値（体重）に表示されていること
* [ ] キーボード(整数)から渡された値に小数点が含まれていないこと

#### 正常 未実施 キーボード(整数)で AC ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(整数)が表示される。
4. AC ボタンを押下する。
* [ ] 入力値（体重）の値がクリアされていること

#### 正常 未実施 キーボード(整数)で確定ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(整数)が表示される。
4. 確定ボタンを押下する。
* [ ] キーボード(整数)が表示されていないこと

#### 正常 未実施 キーボード(整数)以外を押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(整数)が表示される。
4. キーボード(整数)以外を押下する。
* [ ] キーボード(整数)が表示されていないこと

### キーボード(選択)

#### 正常 未実施 キーボード(選択)でカスタムボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(選択)が表示される。
4. カスタムボタンを押下する。
* [ ] キーボード(選択)から渡された値が入力値（体重）に表示されていること

#### 正常 未実施 キーボード(選択)で AC ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(選択)が表示される。
4. AC ボタンを押下する。
* [ ] 入力値（体重）の値がクリアされていること

#### 正常 未実施 キーボード(選択)以外を押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(選択)が表示される。
4. キーボード(選択)以外を押下する。
* [ ] キーボード(選択)が表示されていないこと

## 入力値（体脂肪率）押下

### 入力値（体脂肪率）

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「1」の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
* [ ] キーボード(整数)が表示されること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「1」の時/2回押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(整数)が表示される。
4. 入力値（体脂肪率）を押下する。
* [ ] キーボード(整数)が表示されること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「2」の時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
* [ ] キーボード(選択)が表示されること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「2」の時/2回押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(選択)が表示される。
4. 入力値（体脂肪率）を押下する。
* [ ] キーボード(選択)が表示されること

#### 異常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の keyboard.keyboardType が「2」のkeyboard.keyboardTypeが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
* [ ] キーボード(整数)が表示されること

### キーボード(整数)

#### 正常 未実施 キーボード(整数)で数字ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
* [ ] キーボード(整数)から渡された値が入力値（体脂肪率）に表示されていること

#### 正常 未実施 integerLength、decimalLength が設定されているときにキーボード(整数)で数字ボタンを押下した時
1. value を「"1234"」、integerLength を「3」、decimalLength を「1」に設定する
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. 入力値（体脂肪率）を押下する。
4. キーボード(整数)が表示される。
5. 数字ボタンを押下する。
* [ ] キーボード(整数)から渡された「"123.4"」が入力値（体脂肪率）に表示されていること

#### 正常 未実施 integerLength と decimaLength が設定されており decimaLength が 0 の時に数字ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
* [ ] キーボード(整数)から渡された値が入力値（体脂肪率）に表示されていること
* [ ] キーボード(整数)から渡された値に小数点が含まれていないこと

#### 正常 未実施 キーボード(整数)で AC ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(整数)が表示される。
4. AC ボタンを押下する。
* [ ] 入力値（体脂肪率）の値がクリアされていること

#### 正常 未実施 キーボード(整数)で確定ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(整数)が表示される。
4. 確定ボタンを押下する。
* [ ] キーボード(整数)が表示されていないこと

#### 正常 未実施 キーボード(整数)以外を押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(整数)が表示される。
4. キーボード(整数)以外を押下する。
* [ ] キーボード(整数)が表示されていないこと

### キーボード(選択)

#### 正常 未実施 キーボード(選択)でカスタムボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(選択)が表示される。
4. カスタムボタンを押下する。
* [ ] キーボード(選択)から渡された値が入力値（体脂肪率）に表示されていること

#### 正常 未実施 キーボード(選択)で AC ボタンを押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(選択)が表示される。
4. AC ボタンを押下する。
* [ ] 入力値（体脂肪率）の値がクリアされていること

#### 正常 未実施 キーボード(選択)以外を押下した時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(選択)が表示される。
4. キーボード(選択)以外を押下する。
* [ ] キーボード(選択)が表示されていないこと

## クリアボタン（身長）押下

### クリアボタン（身長）

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. クリアボタン（身長）を押下する。
* [ ] 入力値（身長）の値がクリアされること

## クリアボタン（体重）押下

### クリアボタン（体重）

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. クリアボタン（体重）を押下する。
* [ ] 入力値（体重）の値がクリアされること

## クリアボタン（体脂肪率）押下

### クリアボタン（体脂肪率）

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. クリアボタン（体脂肪率）を押下する。
* [ ] 入力値（体脂肪率）の値がクリアされること

## onRegisterPressed が true になった時

### エラーメッセージ（身長）

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが存在しない時/nameが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「{項目名（身長）}は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが存在しない時/nameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「身長は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが半角数字以外の時/nameが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「{項目名（身長）}は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが半角数字以外の時/nameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「身長は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが examNormalValueRanges の errorLevel:3 に該当する場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「入力に誤りがあります。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが examNormalValueRanges の errorLevel:2 に該当する場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「入力値を確認してください。」が文字色をテーマの"warning"で表示されていること

### エラーメッセージ（体重）

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが存在しない時/nameが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「{項目名（体重）}は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが存在しない時/nameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「体重は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが半角数字以外の時/nameが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「{項目名（体重）}は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが半角数字以外の時/nameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「体重は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが examNormalValueRanges の errorLevel:3 に該当する場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「入力に誤りがあります。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが examNormalValueRanges の errorLevel:2 に該当する場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「入力値を確認してください。」が文字色をテーマの"warning"で表示されていること

### エラーメッセージ（体脂肪率）

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが存在しない時/nameが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「{項目名（体脂肪率）}は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが存在しない時/nameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「体脂肪率は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが半角数字以外の時/nameが存在する時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「{項目名（体脂肪率）}は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが半角数字以外の時/nameが存在しない時
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「体脂肪率は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが examNormalValueRanges の errorLevel:3 に該当する場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「入力に誤りがあります。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の hasOrder が true かつ cancelReasonId が 存在しない場合/valueが examNormalValueRanges の errorLevel:2 に該当する場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
* [ ] エラーメッセージ「入力値を確認してください。」が文字色をテーマの"warning"で表示されていること

## examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の value の値が変更された時

### エラーメッセージ（身長）

#### 正常 未実施 name に値が設定されており value に空の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
5. クリアボタンを押下する。
* [ ] エラーメッセージ「{項目名（身長）}は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 name に値が設定されておらず value に空の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
5. クリアボタンを押下する。
* [ ] エラーメッセージ「身長は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 name に値が設定されており value に半角数字以外の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. デバイスキーボードで「a」を入力する。
* [ ] エラーメッセージ「{項目名（身長）}は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 name に値が設定されておらず value に半角数字以外の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（身長）を押下する。
3. デバイスキーボードで「a」を入力する。
* [ ] エラーメッセージ「身長は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 value が、examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の examNormalValueRanges の errorLevel:3 に該当する場合
1. 引数の examNormalValueRanges に{errorLevel:3, maxValue:100.0,minValue:0.0}を設定する。
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. 入力値（身長）を押下する。
4. 数字ボタンの「1」を押下する。
* [ ] エラーメッセージ「入力値に誤りがあります。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 value が、examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の examNormalValueRanges の errorLevel:2 に該当する場合
1. 引数の examNormalValueRanges に{errorLevel:2, maxValue:100.0,minValue:0.0}を設定する。
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. 入力値（身長）を押下する。
4. 数字ボタンの「1」を押下する。
* [ ] エラーメッセージ「入力値を確認してください。」が文字色をテーマの"warning"で表示されていること

### BMI計算処理：examItems.positionNumberが「1」のexamItemDetails.positionNumberが「1」のhasOrderがtrueかつcancelReasonIdが存在しない時 かつ examItems.positionNumberが「2」のexamItemDetails.positionNumberが「1」のhasOrderがtrueかつcancelReasonIdが存在しない時 かつ examItems.positionNumberが「4」のexamItemDetails.positionNumberが「1」のhasOrderがtrueかつcancelReasonIdが存在しない時

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の valueが0以外の時
1.  examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のintegerLengthに「3」decimalLengthに「0」、examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のintegerLengthに「2」decimalLengthに「0」、examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のintegerLengthに「2」decimalLengthに「2」を設定する。
2.  検査項目グループ_検査：身体計測コンポーネントが呼び出される
3.  入力値（身長）を押下する
4.  数字ボタンを「1」「7」「5」の順番で押下する
5.  入力値（体重）を押下する。
6.  数字ボタンを「7」「5」の順番で押下する
* [ ] 計算値（BMI）に小数第三位（decimalLength+1桁）で四捨五入された「"24.49"」が表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の valueが0の時
1.  examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のintegerLengthに「3」decimalLengthに「0」、examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のintegerLengthに「2」decimalLengthに「0」、examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のintegerLengthに「2」decimalLengthに「2」を設定する。
2.  検査項目グループ_検査：身体計測コンポーネントが呼び出される
3.  入力値（身長）を押下する
4.  数字ボタンの「0」を押下する
5.  入力値（体重）を押下する
6.  数字ボタンを「7」「5」の順番で押下する
* [ ] 計算値（BMI）が空白で表示されていること

### コールバック

#### 正常 未実施 examItems.positionNumberが「1」「2」「3」の要素について、それぞれのexamRegistResultsにバリデーションチェックでerrorLevel:3のエラーメッセージが追加されていない時
1. examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のvalueに「"53"」、examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のvalueに「"24"」を設定する。
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. クリアボタン（身長）を押下する
4. 入力値（身長）を押下する
5. 数字ボタンを「1」「6」「0」の順番で押下する
* [ ] 引数.変更イベントが実行され、examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のvalueに「"160"」がセットされたexamItemsが渡されていること

## examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の value の値が変更された時

### エラーメッセージ（体重）

#### 正常 未実施 name に値が設定されており value に空の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
5. クリアボタンを押下する。
* [ ] エラーメッセージ「{項目名（体重）}は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 name に値が設定されておらず value に空の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
5. クリアボタンを押下する。
* [ ] エラーメッセージ「体重は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 name に値が設定されており value に半角数字以外の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. デバイスキーボードで「a」を入力する。
* [ ] エラーメッセージ「{項目名（体重）}は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 name に値が設定されておらず value に半角数字以外の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体重）を押下する。
3. デバイスキーボードで「a」を入力する。
* [ ] エラーメッセージ「体重は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 value が、examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の examNormalValueRanges の errorLevel:3 に該当する場合
1. 引数の examNormalValueRanges に{errorLevel:3, maxValue:100.0,minValue:0.0}を設定する。
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. 入力値（体重）を押下する。
4. 数字ボタンの「1」を押下する。
* [ ] エラーメッセージ「入力値に誤りがあります。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 value が、examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」の examNormalValueRanges の errorLevel:2 に該当する場合
1. 引数の examNormalValueRanges に{errorLevel:2, maxValue:100.0,minValue:0.0}を設定する。
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. 入力値（体重）を押下する。
4. 数字ボタンの「1」を押下する。
* [ ] エラーメッセージ「入力値を確認してください。」が文字色をテーマの"warning"で表示されていること

### BMI計算処理：examItems.positionNumberが「1」のexamItemDetails.positionNumberが「1」のhasOrderがtrueかつcancelReasonIdが存在しない時 かつ examItems.positionNumberが「2」のexamItemDetails.positionNumberが「1」のhasOrderがtrueかつcancelReasonIdが存在しない時 かつ examItems.positionNumberが「4」のexamItemDetails.positionNumberが「1」のhasOrderがtrueかつcancelReasonIdが存在しない時

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の valueが0以外の時
1.  examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のintegerLengthに「3」decimalLengthに「0」、examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のintegerLengthに「2」decimalLengthに「0」、examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のintegerLengthに「2」decimalLengthに「2」を設定する。
2.  検査項目グループ_検査：身体計測コンポーネントが呼び出される
3.  入力値（身長）を押下する
4.  数字ボタンを「1」「7」「5」の順番で押下する
5.  入力値（体重）を押下する。
6.  数字ボタンを「7」「5」の順番で押下する
* [ ] 計算値（BMI）に小数第三位（decimalLength+1桁）で四捨五入された「"24.49"」が表示されていること

#### 正常 未実施 examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」の valueが0の時
1.  examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のintegerLengthに「3」decimalLengthに「0」、examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のintegerLengthに「2」decimalLengthに「0」、examItems.positionNumber が「4」の examItemDetails.positionNumber が「1」のintegerLengthに「2」decimalLengthに「2」を設定する。
2.  検査項目グループ_検査：身体計測コンポーネントが呼び出される
3.  入力値（身長）を押下する
4.  数字ボタンの「0」を押下する
5.  入力値（体重）を押下する。
6.  数字ボタンを「7」「5」の順番で押下する
* [ ] 計算値（BMI）が空白で表示されていること

### コールバック

#### 正常 未実施 examItems.positionNumberが「1」「2」「3」の要素について、それぞれのexamRegistResultsにバリデーションチェックでerrorLevel:3のエラーメッセージが追加されていない時
1. examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のvalueに「"160"」、examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のvalueに「"24"」を設定する。
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. クリアボタン（体重）を押下する
4. 入力値（体重）を押下する
5. 数字ボタンを「5」「3」の順番で押下する
* [ ] 引数.変更イベントが実行され、examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のvalueに「"53"」がセットされたexamItemsが渡されていること

## examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の value の値が変更された時

### エラーメッセージ（体脂肪率）

#### 正常 未実施 name に値が設定されており value に空の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
5. クリアボタンを押下する。
* [ ] エラーメッセージ「{項目名（体脂肪率）}は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 name に値が設定されておらず value に空の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. キーボード(整数)が表示される。
4. 数字ボタンを押下する。
5. クリアボタンを押下する。
* [ ] エラーメッセージ「体脂肪率は入力必須項目です。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 name に値が設定されており value に半角数字以外の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. デバイスキーボードで「a」を入力する。
* [ ] エラーメッセージ「{項目名（体脂肪率）}は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 name に値が設定されておらず value に半角数字以外の値が設定された場合
1. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
2. 入力値（体脂肪率）を押下する。
3. デバイスキーボードで「a」を入力する。
* [ ] エラーメッセージ「体脂肪率は半角数字で入力してください。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 value が、examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の examNormalValueRanges の errorLevel:3 に該当する場合
1. 引数の examNormalValueRanges に{errorLevel:3, maxValue:100.0,minValue:0.0}を設定する。
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. 入力値（体脂肪率）を押下する。
4. 数字ボタンの「1」を押下する。
* [ ] エラーメッセージ「入力値に誤りがあります。」が文字色をテーマの"error"で表示されていること

#### 正常 未実施 value が、examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」の examNormalValueRanges の errorLevel:2 に該当する場合
1. 引数の examNormalValueRanges に{errorLevel:2, maxValue:100.0,minValue:0.0}を設定する。
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. 入力値（体脂肪率）を押下する。
4. 数字ボタンの「1」を押下する。
* [ ] エラーメッセージ「入力値を確認してください。」が文字色をテーマの"warning"で表示されていること

### コールバック

#### 正常 未実施 examItems.positionNumberが「1」「2」「3」の要素について、それぞれのexamRegistResultsにバリデーションチェックでerrorLevel:3のエラーメッセージが追加されていない時
1. examItems.positionNumber が「1」の examItemDetails.positionNumber が「1」のvalueに「160」、examItems.positionNumber が「2」の examItemDetails.positionNumber が「1」のvalueに「"53"」を設定する。
2. 検査項目グループ_検査：身体計測コンポーネントが呼び出される
3. クリアボタン（体脂肪率）を押下する
4. 入力値（体脂肪率）を押下する
5. 数字ボタンを「2」「4」の順番で押下する
* [ ] 引数.変更イベントが実行され、examItems.positionNumber が「3」の examItemDetails.positionNumber が「1」のvalueに「"24"」がセットされたexamItemsが渡されていること

