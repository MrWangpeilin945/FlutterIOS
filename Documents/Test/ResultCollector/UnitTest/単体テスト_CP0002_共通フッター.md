# CP0002_共通フッター

## 初期表示

### ボタン

#### 正常 未実施 引数の要素数が0個
1. 共通フッターコンポーネントが呼び出される
* [ ] 戻るボタンが表示されていること
* [ ] 要素1はボタンのラベルが空白で表示されていること
* [ ] 要素2はボタンのラベルが空白で表示されていること
* [ ] 要素3はボタンのラベルが空白で表示されていること

#### 正常 未実施 引数の要素数が1個
1. 共通フッターコンポーネントが呼び出される
* [ ] 戻るボタンが表示されていること
* [ ] 要素1は引数で渡された[0]番目の要素からボタンのラベルが表示されていること
* [ ] 要素2はボタンのラベルが空白で表示されていること
* [ ] 要素3はボタンのラベルが空白で表示されていること

#### 正常 未実施 引数の要素数が2個
1. 共通フッターコンポーネントが呼び出される
* [ ] 戻るボタンが表示されていること
* [ ] 要素1は引数で渡された[0]番目の要素からボタンのラベルが表示されていること
* [ ] 要素2は引数で渡された[1]番目の要素からボタンのラベルが表示されていること
* [ ] 要素3はボタンのラベルが空白で表示されていること

#### 正常 未実施 引数の要素数が3個
1. 共通フッターコンポーネントが呼び出される
* [ ] 戻るボタンが表示されていること
* [ ] 要素1は引数で渡された[0]番目の要素からボタンのラベルが表示されていること
* [ ] 要素2は引数で渡された[1]番目の要素からボタンのラベルが表示されていること
* [ ] 要素3は引数で渡された[2]番目の要素からボタンのラベルが表示されていること

#### 正常 未実施 引数の要素数が4個
1. 共通フッターコンポーネントが呼び出される
* [ ] 戻るボタンが表示されていること
* [ ] 要素1は引数で渡された[0]番目の要素からボタンのラベルが表示されていること
* [ ] 要素2は引数で渡された[1]番目の要素からボタンのラベルが表示されていること
* [ ] 要素3は引数で渡された[2]番目の要素からボタンのラベルが表示されていること
* [ ] [3]番目の要素についてはボタンが表示されていないこと

## 戻るボタン押下

### 画面遷移

#### 正常 未実施 　
1. 共通フッターコンポーネントが呼び出される
2. 戻るボタンを押下する
* [ ] ひとつ前の画面に遷移すること

## 要素1ボタン押下

### 画面遷移

#### 正常 未実施 引数の要素数が0個
1. 共通フッターコンポーネントが呼び出される
2. 要素1ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が1個 かつ 要素[0]番目に押下時のイベント設定がある
1. 共通フッターコンポーネントが呼び出される
2. 要素1ボタンを押下する
* [ ] 要素[0]番目の押下時イベントが実行されること

#### 正常 未実施 引数の要素数が1個 かつ 要素[0]番目に押下時のイベント設定がない
1. 共通フッターコンポーネントが呼び出される
2. 要素1ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が2個 かつ 要素[0]番目に押下時のイベント設定がある
1. 共通フッターコンポーネントが呼び出される
2. 要素1ボタンを押下する
* [ ] 要素[0]番目の押下時イベントが実行されること

#### 正常 未実施 引数の要素数が2個 かつ 要素[0]番目に押下時のイベント設定がない
1. 共通フッターコンポーネントが呼び出される
2. 要素1ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が3個 かつ 要素[0]番目に押下時のイベント設定がある
1. 共通フッターコンポーネントが呼び出される
2. 要素1ボタンを押下する
* [ ] 要素[0]番目の押下時イベントが実行されること

#### 正常 未実施 引数の要素数が3個 かつ 要素[0]番目に押下時のイベント設定がない
1. 共通フッターコンポーネントが呼び出される
2. 要素1ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が4個 かつ 要素[0]番目に押下時のイベント設定がある
1. 共通フッターコンポーネントが呼び出される
2. 要素1ボタンを押下する
* [ ] 要素[0]番目の押下時イベントが実行されること

#### 正常 未実施 引数の要素数が4個 かつ 要素[0]番目に押下時のイベント設定がない
1. 共通フッターコンポーネントが呼び出される
2. 要素1ボタンを押下する
* [ ] 何も処理されないこと

## 要素2ボタン押下

### 画面遷移

#### 正常 未実施 引数の要素数が0個
1. 共通フッターコンポーネントが呼び出される
2. 要素2ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が1個
1. 共通フッターコンポーネントが呼び出される
2. 要素2ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が2個 かつ 要素[1]番目に押下時のイベント設定がある
1. 共通フッターコンポーネントが呼び出される
2. 要素2ボタンを押下する
* [ ] 要素[1]番目の押下時イベントが実行されること

#### 正常 未実施 引数の要素数が2個 かつ 要素[1]番目に押下時のイベント設定がない
1. 共通フッターコンポーネントが呼び出される
2. 要素2ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が3個 かつ 要素[1]番目に押下時のイベント設定がある
1. 共通フッターコンポーネントが呼び出される
2. 要素2ボタンを押下する
* [ ] 要素[1]番目の押下時イベントが実行されること

#### 正常 未実施 引数の要素数が3個 かつ 要素[1]番目に押下時のイベント設定がない
1. 共通フッターコンポーネントが呼び出される
2. 要素2ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が4個 かつ 要素[1]番目に押下時のイベント設定がある
1. 共通フッターコンポーネントが呼び出される
2. 要素2ボタンを押下する
* [ ] 要素[1]番目の押下時イベントが実行されること

#### 正常 未実施 引数の要素数が4個 かつ 要素[1]番目に押下時のイベント設定がない
1. 共通フッターコンポーネントが呼び出される
2. 要素2ボタンを押下する
* [ ] 何も処理されないこと

## 要素3ボタン押下

### 画面遷移

#### 正常 未実施 引数の要素数が0個
1. 共通フッターコンポーネントが呼び出される
2. 要素3ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が1個
1. 共通フッターコンポーネントが呼び出される
2. 要素3ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が2個
1. 共通フッターコンポーネントが呼び出される
2. 要素3ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が3個 かつ 要素[2]番目に押下時のイベント設定がある
1. 共通フッターコンポーネントが呼び出される
2. 要素3ボタンを押下する
* [ ] 要素[2]番目の押下時イベントが実行されること

#### 正常 未実施 引数の要素数が3個 かつ 要素[2]番目に押下時のイベント設定がない
1. 共通フッターコンポーネントが呼び出される
2. 要素3ボタンを押下する
* [ ] 何も処理されないこと

#### 正常 未実施 引数の要素数が4個 かつ 要素[2]番目に押下時のイベント設定がある
1. 共通フッターコンポーネントが呼び出される
2. 要素3ボタンを押下する
* [ ] 要素[2]番目の押下時イベントが実行されること

#### 正常 未実施 引数の要素数が4個 かつ 要素[2]番目に押下時のイベント設定がない
1. 共通フッターコンポーネントが呼び出される
2. 要素3ボタンを押下する
* [ ] 何も処理されないこと
