# CP1010_検査項目グループ_検査:選択（左右）

## 初期表示　

### データ確認

#### 異常 未実施 引数.検査項目情報のexamItemsが存在しない時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：選択（左右）コンポーネントが表示されていないこと

#### 異常 未実施 引数.検査項目情報のexamItemsに要素が存在しない時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：選択（左右）コンポーネントが表示されていないこと

#### 異常 未実施 引数.検査項目情報のexamItemsにpositionNumberが「1」の要素が存在しない時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：選択（左右）コンポーネントが表示されていないこと

#### 異常 未実施 引数.検査項目情報のexamItemsにpositionNumberが「1」の要素のexamItemDetailsの中にpositionNumberが「1」と「2」の要素が両方存在しない時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：選択（左右）コンポーネントが表示されていないこと

#### 正常 未実施 引数.検査項目情報のexamItemsにpositionNumberが「1」の要素のexamItemDetailsの中にpositionNumberが「1」の要素が存在する時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：選択（左右）コンポーネントが表示されていること

#### 正常 未実施 引数.検査項目情報のexamItemsにpositionNumberが「1」の要素のexamItemDetailsの中にpositionNumberが「2」の要素が存在する時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 検査項目グループ_検査：選択（左右）コンポーネントが表示されていること

### 検査項目

#### 異常 未実施 examItems.positionNumberが「1」のnameが存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 検査項目ラベルが表示されていないこと

#### 正常 未実施 examItems.positionNumberが「1」のnameが存在する時/nameが8文字以下の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItems.positionNumberが「1」のnameが表示されていること

#### 正常 未実施 examItems.positionNumberが「1」のnameが存在する時/nameが9文字以上の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItems.positionNumberが「1」のnameが、先頭から8文字表示されていること

### 左明細名

#### 正常 未実施 examItemDetail.positionNumberが「1」のnameが存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 「"左"」が表示されていること

#### 正常 未実施 examItemDetail.positionNumberが「1」のnameが存在する時/nameが14文字以下の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItemDetails.positionNumberが「1」のnameが表示されていること

#### 正常 未実施 examItemDetail.positionNumberが「1」のnameが存在する時/nameが15文字以上の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItemDetails.positionNumberが「1」のnameが先頭から14文字表示されていること

### 左前回値

#### 正常 未実施 examItemDetail.positionNumberが「1」のprevValueが存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 左前回値が表示されていないこと

#### 正常 未実施 examItemDetail.positionNumberが「1」のprevValueが存在する時/examItemDetailOptionsにprevValueと等しいcodeの要素が存在する時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItemDetails.positionNumberが「1」のprevValueと等しいexamItemDetailOptions.codeの要素に対応するnameが表示されていること

#### 異常 未実施 examItemDetail.positionNumberが「1」のprevValueが存在する時/examItemDetailOptionsにprevValueと等しいcodeの要素が存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItemDetails.positionNumberが「1」のprevValueが表示されていること

### 左選択ボタン

#### 異常 未実施 examItemDetails.positionNumberが「1」のexamItemDetailOptionsが存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 左選択ボタンが表示されないこと

#### 正常 未実施 examItemDetails.positionNumberが「1」のexamItemDetailOptionsが存在する時/examItemDetailOptions.nameが8文字以下の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 選択ボタンがexamItemDetails.positionNumberが「1」のexamItemDetailOptionsの要素数分表示されること
* [ ] 選択ボタンのラベルのラベルにexamItemDetailOptions.nameが表示されること

#### 正常 未実施 examItemDetails.positionNumberが「1」のexamItemDetailOptionsが存在する時/examItemDetailOptions.nameが9文字以上の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 選択ボタンがexamItemDetails.positionNumberが「1」のexamItemDetailOptionsの要素数分表示されること
* [ ] 各選択ボタンのラベルにexamItemDetailOptions.nameが先頭から8文字表示されること

#### 正常 未実施 examItemDetails.positionNumberが「1」のexamItemDetailOptionsが存在する時/hasOrderがfalseの時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 左選択ボタンがexamItemDetails.positionNumberが「1」のexamItemDetailOptionsの要素数分表示されること
* [ ] 左選択ボタンがグレーアウト（選択不可）で表示されること

#### 正常 未実施 examItemDetails.positionNumberが「1」のexamItemDetailOptionsが存在する時/cancelReasonIdが存在する時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 左選択ボタンがexamItemDetails.positionNumberが「1」のexamItemDetailOptionsの要素数分表示されること
* [ ] 左選択ボタンがグレーアウト（選択不可）で表示されること

#### 正常 未実施 examItemDetails.positionNumberが「1」のexamItemDetailOptionsが存在する時/valueが存在する時/examItemDetailOptionsにvalueと等しいcodeの要素が存在する時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 左選択ボタンがexamItemDetails.positionNumberが「1」のexamItemDetailOptionsの要素数分表示されること
* [ ] valueとexamItemDetailOptions.codeが等しいボタンが、文字色をテーマの"primary"、背景色をテーマの"green03"で表示されること

### 右明細名

#### 正常 未実施 examItemDetail.positionNumberが「2」のnameが存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 「"右"」が表示されていること

#### 正常 未実施 examItemDetail.positionNumberが「2」のnameが存在する時/nameが14文字以下の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItemDetails.positionNumberが「2」のnameが表示されていること

#### 正常 未実施 examItemDetail.positionNumberが「2」のnameが存在する時/nameが15文字以上の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItemDetails.positionNumberが「2」のnameが先頭から14文字表示されていること

### 右前回値

#### 正常 未実施 examItemDetail.positionNumberが「2」のprevValueが存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 左前回値が表示されていないこと

#### 正常 未実施 examItemDetail.positionNumberが「2」のprevValueが存在する時/examItemDetailOptionsにprevValueと等しいcodeの要素が存在する時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItemDetails.positionNumberが「2」のprevValueと等しいexamItemDetailOptions.codeの要素に対応するnameが表示されていること

#### 異常 未実施 examItemDetail.positionNumberが「2」のprevValueが存在する時/examItemDetailOptionsにprevValueと等しいcodeの要素が存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 引数.検査項目情報のexamItemDetails.positionNumberが「2」のprevValueが表示されていること

### 右選択ボタン

#### 異常 未実施 examItemDetails.positionNumberが「2」のexamItemDetailOptionsが存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 右選択ボタンが表示されないこと

#### 正常 未実施 examItemDetails.positionNumberが「2」のexamItemDetailOptionsが存在する時/examItemDetailOptions.nameが8文字以下の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 選択ボタンがexamItemDetails.positionNumberが「2」のexamItemDetailOptionsの要素数分表示されること
* [ ] 選択ボタンのラベルのラベルにexamItemDetailOptions.nameが表示されること

#### 正常 未実施 examItemDetails.positionNumberが「2」のexamItemDetailOptionsが存在する時/examItemDetailOptions.nameが9文字以上の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 選択ボタンがexamItemDetails.positionNumberが「2」のexamItemDetailOptionsの要素数分表示されること
* [ ] 各選択ボタンのラベルにexamItemDetailOptions.nameが先頭から8文字表示されること

#### 正常 未実施 examItemDetails.positionNumberが「2」のexamItemDetailOptionsが存在する時/hasOrderがfalseの時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 右選択ボタンがexamItemDetails.positionNumberが「2」のexamItemDetailOptionsの要素数分表示されること
* [ ] 右選択ボタンがグレーアウト（選択不可）で表示されること

#### 正常 未実施 examItemDetails.positionNumberが「2」のexamItemDetailOptionsが存在する時/cancelReasonIdが存在する時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 右選択ボタンがexamItemDetails.positionNumberが「2」のexamItemDetailOptionsの要素数分表示されること
* [ ] 右選択ボタンがグレーアウト（選択不可）で表示されること

#### 正常 未実施 examItemDetails.positionNumberが「2」のexamItemDetailOptionsが存在する時/valueが存在する時/examItemDetailOptionsにvalueと等しいcodeの要素が存在する時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] 右選択ボタンがexamItemDetails.positionNumberが「2」のexamItemDetailOptionsの要素数分表示されること
* [ ] valueとexamItemDetailOptions.codeが等しいボタンが、文字色をテーマの"primary"、背景色をテーマの"green03"で表示されること

### エラーメッセージ

#### 正常 未実施 examItems.positionNumberが「1」のexamRegistResultsが存在しない時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] エラーメッセージが表示されないこと

#### 正常 未実施 examItems.positionNumberが「1」のexamRegistResultsが存在する時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] examRegistResultsの要素数分エラーメッセージが表示されること
* [ ] examRegistResultsの各要素のerrorLevelの降順で表示されていること

#### 正常 未実施 examItems.positionNumberが「1」のexamRegistResultsが存在する時/examRegistResults.errorLevelが2の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"warning"の色で表示されること

#### 正常 未実施 examItems.positionNumberが「1」のexamRegistResultsが存在する時/examRegistResults.errorLevelが3の時 　
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] エラーメッセージの文字色がテーマの"error"の色で表示されること

#### 正常 未実施 引数.onRegisterPressedがfalseの時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] エラーメッセージ「{左明細名}は入力必須項目です。」と「{右明細名}は入力必須項目です。」が表示されないこと

## onRegisterPressedがtrueに変更された時

### エラーメッセージ

#### 正常 未実施 examItems.positionNumberが「1」内のexamItemDetails.positionNumberが「1」のvalueが存在しない時/hasOrderがtrue かつ cancelReasonIdが存在しない時/examItemDetails.positioNumberが「1」のnameが存在する時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] エラーメッセージ「{左明細名}は入力必須項目です。」が文字色をテーマの"error"で表示されること

#### 異常 未実施 examItems.positionNumberが「1」内のexamItemDetails.positionNumberが「1」のvalueが存在しない時/hasOrderがtrue かつ cancelReasonIdが存在しない時/examItemDetails.positioNumberが「1」のnameが存在しない時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] エラーメッセージ「左は入力必須項目です。」が文字色をテーマの"error"で表示されること

#### 正常 未実施 examItems.positionNumberが「1」内のexamItemDetails.positionNumberが「2」のvalueが存在しない時/hasOrderがtrue かつ cancelReasonIdが存在しない時/examItemDetails.positioNumberが「2」のnameが存在する時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] エラーメッセージ「{右明細名}は入力必須項目です。」が文字色をテーマの"error"で表示されること

#### 異常 未実施 examItems.positionNumberが「1」内のexamItemDetails.positionNumberが「2」のvalueが存在しない時/hasOrderがtrue かつ cancelReasonIdが存在しない時/examItemDetails.positioNumberが「2」のnameが存在しない時
1. 検査項目グループ_検査：選択（左右）コンポーネントが呼び出される
* [ ] エラーメッセージ「右は入力必須項目です。」が文字色をテーマの"error"で表示されること

## 選択ボタン押下時

### 左選択ボタン

#### 正常 未実施 「選択済み」に変更した時 　
1. 未選択の左選択ボタンを押下
* [ ] 選択した左選択ボタンが、文字色をテーマの"primary"、背景色を"green03"で表示されること
* [ ] 選択した左選択ボタン以外は、文字色をテーマの"black"、背景色を"white"で表示されること
* [ ] 引数.変更イベントが実行され、押下したボタンのcodeがexamiItems.positionNumberが「1」内のexamItemDetail.positionNumberが「1」のvalueにセットされたexamItemsが渡されること

#### 正常 未実施 「未選択」に変更した時 　
1. 選択済みの左選択ボタンを押下
* [ ] 選択した左選択ボタンが、文字色をテーマの"black"、背景色を"white"で表示される
* [ ] エラーメッセージ「{左明細名}は入力必須項目です。」が文字色をテーマの"error"で表示されること
* [ ] 引数.変更イベントが実行されないこと

### 右選択ボタン

#### 正常 未実施 「選択済み」に変更した時 　
1. 未選択の右選択ボタンを押下
* [ ] 選択した右選択ボタンが、文字色をテーマの"primary"、背景色を"green03"で表示されること
* [ ] 選択した右選択ボタン以外は、文字色をテーマの"black"、背景色を"white"で表示されること
* [ ] 引数.変更イベントが実行され、押下したボタンのcodeがexamiItems.positionNumberが「1」内のexamItemDetail.positionNumberが「2」のvalueにセットされたexamItemsが渡されること

#### 正常 未実施 「未選択」に変更した時 　
1. 選択済みの右選択ボタンを押下
* [ ] 選択した右選択ボタンが、文字色をテーマの"black"、背景色を"white"で表示される
* [ ] エラーメッセージ「{右明細名}は入力必須項目です。」が文字色をテーマの"error"で表示されること
* [ ] 引数.変更イベントが実行されないこと