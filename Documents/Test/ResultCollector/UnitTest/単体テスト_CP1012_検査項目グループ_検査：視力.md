# CP1012_検査項目グループ_検査：視力

## 大項目
### 中項目
#### [正常|異常|準正常] [OK|NG|未実施|--] テストケース名
1. 確認手順
2. 確認手順
* [ ] 想定動作
* [ ] 想定動作
- 備考

## 初期表示

### データ確認

#### 異常 未実施 引数.検査項目情報の examItems が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 視力コンポーネントが表示されない。

#### 異常 未実施 引数.検査項目情報の examItems に要素が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 視力コンポーネントが表示されない。

#### 異常 未実施 引数.検査項目情報の examItems に要素は存在するが、要素のpositionNumber「4」のみ存在する場合
1. 視力コンポーネントが呼び出される
* [ ] 視力コンポーネントが表示されない。
- ※視力コンポーネントにexamItems[x].positionNumber=4は存在しない。

#### 正常 未実施 引数.検査項目情報の examItems に要素は存在するが、要素のpositionNumber「1」（裸眼）のみ存在する場合
1. 視力コンポーネントが呼び出される
* [ ] 視力コンポーネントが表示される。

#### 正常 未実施 引数.検査項目情報の examItems に要素は存在するが、要素のpositionNumber「2」（矯正）のみ存在する場合
1. 視力コンポーネントが呼び出される
* [ ] 視力コンポーネントが表示される。

#### 正常 未実施 引数.検査項目情報の examItems に要素は存在するが、要素のpositionNumber「3」（特記）のみ存在する場合
1. 視力コンポーネントが呼び出される
* [ ] 視力コンポーネントが表示される。


### 検査項目・検査項目明細のテスト

#### 正常 未実施 以降のテストは、視力コンポーネントは表示される条件を満たしている状態で行う↓　


### 検査項目ラベル表示

#### 正常 未実施 裸眼の表示 引数.検査項目情報.examItem の positionNumberが「1」（裸眼）の要素の name が設定されている場合
1. 視力コンポーネントが呼び出される
* [ ] 「裸眼」の固定値が表示される
- ※ラベル固定値

#### 正常 未実施 裸眼の表示 引数.検査項目情報.examItem の positionNumberが「2」（矯正）の要素の name が設定されている場合
1. 視力コンポーネントが呼び出される
* [ ] 「矯正」の固定値が表示される
- ※ラベル固定値

#### 正常 未実施 裸眼の表示 引数.検査項目情報.examItem の positionNumberが「3」（特記）の要素の name が設定されている場合
1. 視力コンポーネントが呼び出される
* [ ] 「特記」の固定値が表示される
- ※ラベル固定値


### 検査項目明細ラベル表示

#### 正常 未実施 検査項目明細ラベルの表示（視力：左）
1. 視力コンポーネントが呼び出される
* [ ] 「視力　左」の固定値が表示される
- ※ラベル固定値

#### 正常 未実施 検査項目明細ラベルの表示（視力：右）
1. 視力コンポーネントが呼び出される
* [ ] 「視力　右」の固定値が表示される
- ※ラベル固定値

#### 正常 未実施 検査項目明細ラベルの表示（視力：両眼）
1. 視力コンポーネントが呼び出される
* [ ] 「視力　両眼」の固定値が表示される
- ※ラベル固定値


### 裸眼　入力値テキストボックス　Enable

#### 正常 未実施 引数.検査項目情報の examItems にpositionNumber が「1」（裸眼）の要素が存在しない場合
1. 視力コンポーネントが呼び出される	
* [ ] 検査項目：裸眼の入力値テキストボックスが全てグレーアウトされる。
* [ ] 裸眼の入力値テキストボックスは入力不可。

#### 正常 未実施 以降のテストは、引数.検査項目情報の examItems にpositionNumber が「1」（裸眼）の要素が存在する条件で行う↓

#### 正常 未実施 裸眼の　examItemDetails が存在しない場合
1. 視力コンポーネントが呼び出される	
* [ ] 検査項目：裸眼の入力値テキストボックスが全てグレーアウトされる。
* [ ] 裸眼の入力値テキストボックスは入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「1」（裸眼：左）が存在しない場合
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（左）の入力値テキストボックスがグレーアウトされる。
* [ ] 裸眼（左）の入力値テキストボックスが入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「1」（裸眼：左）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（左）の入力値テキストボックスがグレーアウトされる。
* [ ] 裸眼（左）の入力値テキストボックスが入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「1」（裸眼：左）が存在する場合（cancelReasonId に値が存在する時）
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（左）の入力値テキストボックスがグレーアウトされる。
* [ ] 裸眼（左）の入力値テキストボックスが入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「1」（裸眼：左）が存在する場合 （hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（左）の入力値テキストボックスがグレーアウトされず表示される。
* [ ] 裸眼（左）の入力値テキストボックスが入力可能。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「2」（裸眼：右）が存在しない場合
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（右）の入力値テキストボックスがグレーアウトされる。
* [ ] 裸眼（右）の入力値テキストボックスが入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「2」（裸眼：右）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（右）の入力値テキストボックスがグレーアウトされる。
* [ ] 裸眼（右）の入力値テキストボックスが入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「2」（裸眼：右）が存在する場合（cancelReasonId に値が存在する時）
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（右）の入力値テキストボックスがグレーアウトされる。
* [ ] 裸眼（右）の入力値テキストボックスが入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「2」（裸眼：右）が存在する場合（hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（右）の入力値テキストボックスがグレーアウトされず表示される。
* [ ] 裸眼（右）の入力値テキストボックスが入力可能。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「3」（裸眼：両眼）が存在しない場合
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（両眼）の入力値テキストボックスがグレーアウトされる。
* [ ] 裸眼（両眼）の入力値テキストボックスが入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「3」（裸眼：両眼）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（両眼）の入力値テキストボックスがグレーアウトされる。
* [ ] 裸眼（両眼）の入力値テキストボックスが入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「3」（裸眼：両眼）が存在する場合（cancelReasonId に値が存在する時）
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（両眼）の入力値テキストボックスがグレーアウトされる。
* [ ] 裸眼（両眼）の入力値テキストボックスが入力不可。

#### 正常 未実施 裸眼の　examItemDetails の中に positionNumber が「3」（裸眼：両眼）が存在する場合（hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される	
* [ ] 裸眼（両眼）の入力値テキストボックスがグレーアウトされず表示される。
* [ ] 裸眼（両眼）の入力値テキストボックスが入力可能。


### 裸眼　入力値テキストボックス　カラー

#### 正常 未実施 裸眼にエラーがない場合。
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（左・右・両眼）の入力値テキストボックスがデフォルトカラーで表示される

#### 正常 未実施 裸眼にエラーがある場合。　裸眼の examRegistResults に含まれる errorLevel の最大値が 3 （エラー）の場合
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（左・右・両眼）の入力値テキストボックスがテーマ"error"の色で表示される。

#### 正常 未実施 裸眼にエラーがある場合。　裸眼の examRegistResults に含まれる errorLevel の最大値が 2 （ワーニング）の場合
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（左・右・両眼）の入力値テキストボックスがテーマ"warning"の色で表示される。


### 裸眼　入力値テキストボックス　値


#### 正常 未実施 以降のテストは、引数.検査項目情報の examItems の positionNumber が「1」（裸眼）の要素の hasOrder = true かつ cancelReasonId に値が存在しない条件で行う↓			

#### 正常 未実施 裸眼（左）の値が空の場合。　裸眼の examItemDetails の中の  positionNumber が「1」の要素（裸眼：左）の value が数値以外の場合（空）
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（左）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 裸眼（左）の値が不正な場合。　裸眼の examItemDetails の中の  positionNumber が「1」の要素（裸眼：左）の value が数値以外の場合（undefined）
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（左）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 裸眼（右）の値が空の場合。　裸眼の examItemDetails の中の  positionNumber が「2」の要素（裸眼：右）の value が数値以外の場合（空）
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（右）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 裸眼（右）の値が不正な場合。　裸眼の examItemDetails の中の  positionNumber が「2」の要素（裸眼：右）の value が数値以外の場合（undefined）
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（右）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 裸眼（両眼）の値が空の場合。　裸眼の examItemDetails の中の  positionNumber が「3」の要素（裸眼：両眼）の value が数値以外の場合（空）
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（両眼）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 裸眼（両眼）の値が不正な場合。　裸眼の examItemDetails の中の  positionNumber が「3」の要素（裸眼：両眼）の value が数値以外の場合（undefined）
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（両眼）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 裸眼（左）の値が数値の場合。　裸眼の examItemDetails の中の  positionNumber が「1」の要素（裸眼：左）の value が数値の場合
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（左）の入力値テキストボックスに value の値が表示される。

#### 正常 未実施 裸眼（右）の値が数値の場合。　裸眼の examItemDetails の中の  positionNumber が「2」の要素（裸眼：右）の value が数値の場合
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（右）の入力値テキストボックスに value の値が表示される。

#### 正常 未実施 裸眼（両眼）の値が数値の場合。　裸眼の examItemDetails の中の  positionNumber が「3」の要素（裸眼：両眼）の value が数値の場合
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（両眼）の入力値テキストボックスに value の値が表示される。


### 矯正


#### 正常 未実施 引数.検査項目情報の examItems にpositionNumber が「2」（矯正）の要素が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 検査項目：矯正の選択ボタンと入力値テキストボックスが全てグレーアウトされる。
* [ ] 矯正の選択ボタンは選択不可。
* [ ] 矯正の入力値テキストボックスは入力不可。

#### 正常 未実施 以降のテストは、引数.検査項目情報の examItems にpositionNumber が「2」（矯正）の要素が存在する条件で行う↓

#### 正常 未実施 矯正の　examItemDetails が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 検査項目：矯正の選択ボタンと入力値テキストボックスが全てグレーアウトされる。
* [ ] 矯正区分の選択ボタンは選択不可。
* [ ] 矯正の入力値テキストボックスは入力不可。


### 矯正区分　選択ボタン　Enable


#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「1」（矯正区分：左）が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（左）の選択ボタンは選択不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「1」（矯正区分：左）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（左）の選択ボタンは選択不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「1」（矯正区分：左）が存在する場合（cancelReasonId に値が存在する時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（左）の選択ボタンは選択不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「1」（矯正区分：左）が存在する場合（hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の選択ボタンがグレーアウトされずに表示される。
* [ ] 矯正区分（左）の選択ボタンは選択可能。
- ※ボタンごとのグレーアウトテストは以降で行う。

#### 正常 未実施 以降のテストは、矯正の examItemsDetails の positionNumber が「1」（矯正区分：左）の要素の hasOrder = true かつ cancelReasonId に値が存在しない条件で行う↓

#### 異常 未実施 矯正区分：左の　examItemDetailOptions が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（左）の選択ボタンは選択不可。

#### 異常 未実施 矯正区分：左の　examItemDetailOptions は存在するが、その要素が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（左）の選択ボタンは選択不可。

#### 正常 未実施 矯正区分：左の examItemDetailOptions は存在するが、その要素にorderNumber が「1」（メガネ）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の選択ボタンの１つ目（メガネ）がグレーアウトされる。
* [ ] 矯正区分（左）の選択ボタン１つ目は選択不可。

#### 正常 未実施 矯正区分：左の examItemDetailOptions は存在するが、その要素にorderNumber が「2」（コンタクト）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の選択ボタンの２つ目（コンタクト）がグレーアウトされる。
* [ ] 矯正区分（左）の選択ボタン２つ目は選択不可。
- ※優先度低。

#### 正常 未実施 矯正区分：左の examItemDetailOptions は存在するが、その要素にorderNumber が「1」「2」が存在する
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の選択ボタンの１つ目と２つ目がグレーアウトされずに表示される。
* [ ] 矯正区分（左）の選択ボタンは選択可能。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「2」（矯正区分：右）が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（右）の選択ボタンは選択不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「2」（矯正区分：右）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（右）の選択ボタンは選択不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「2」（矯正区分：右）が存在する場合（cancelReasonId に値が存在する時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（右）の選択ボタンは選択不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「2」（矯正区分：右）が存在する場合（hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の選択ボタンがグレーアウトされずに表示される。
* [ ] 矯正区分（右）の選択ボタンは選択可能。
- ※ボタンごとのグレーアウトテストは以降で行う。

#### 正常 未実施 以降のテストは、矯正の examItemsDetails の positionNumber が「2」（矯正区分：右）の要素の hasOrder = true かつ cancelReasonId に値が存在しない条件で行う↓

#### 異常 未実施 矯正区分：右の　examItemDetailOptions が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（右）の選択ボタンは選択不可。

#### 異常 未実施 矯正区分：右の　examItemDetailOptions は存在するが、その要素が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の選択ボタンがグレーアウトされる。
* [ ] 矯正区分（右）の選択ボタンは選択不可。

#### 正常 未実施 矯正区分：右の examItemDetailOptions は存在するが、その要素にorderNumber が「1」（メガネ）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の選択ボタンの１つ目（メガネ）がグレーアウトされる。
* [ ] 矯正区分（右）の選択ボタン１つ目は選択不可。

#### 正常 未実施 矯正区分：右の examItemDetailOptions は存在するが、その要素にorderNumber が「2」（コンタクト）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の選択ボタンの２つ目（コンタクト）がグレーアウトされる。
* [ ] 矯正区分（右）の選択ボタン２つ目は選択不可。
- ※優先度低。

#### 正常 未実施 矯正区分：右の examItemDetailOptions は存在するが、その要素にorderNumber が「1」「2」が存在する
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の選択ボタンの１つ目と２つ目がグレーアウトされずに表示される。
* [ ] 矯正区分（右）の選択ボタンは選択可能。


### 矯正区分　選択ボタン　値


#### 正常 未実施 矯正区分：左の examItemDetailOptions.orderNumber が「1」のnameが空、もしくはundefinedの場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の１つ目のボタンに「メガネ」と表示される

#### 正常 未実施 矯正区分：左の examItemDetailOptions は存在するが、その要素にorderNumber が「1」（メガネ）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の１つ目のボタンに「メガネ」と表示される

#### 正常 未実施 矯正区分：左の examItemDetailOptions.orderNumber が「1」のnameが7文字以下の時
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の１つ目のボタンに examItemDetailOptions.orderNumber が「1」のnameが表示される

#### 正常 未実施 矯正区分：左の examItemDetailOptions.orderNumber が「1」のnameが8文字以上の時
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の１つ目のボタンに examItemDetailOptions.orderNumber が「1」のnameが先頭から7文字表示される

#### 正常 未実施 矯正区分：左の examItemDetailOptions.orderNumber が「2」のnameが空、もしくはundefinedの場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の2つ目のボタンに「コンタクト」と表示される

#### 正常 未実施 矯正区分：左の examItemDetailOptions は存在するが、その要素にorderNumber が「2」（コンタクト）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の2つ目のボタンに「コンタクト」と表示される

#### 正常 未実施 矯正区分：左の examItemDetailOptions.orderNumber が「2」のnameが7文字以下の時
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の2つ目のボタンに examItemDetailOptions.orderNumber が「2」のnameが表示される

#### 正常 未実施 矯正区分：左の examItemDetailOptions.orderNumber が「2」のnameが8文字以上の時
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）の2つ目のボタンに examItemDetailOptions.orderNumber が「2」のnameが先頭から7文字表示される

#### 正常 未実施 矯正区分：右の examItemDetailOptions.orderNumber が「1」のnameが空、もしくはundefinedの場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の１つ目のボタンに「メガネ」と表示される

#### 正常 未実施 矯正区分：右の examItemDetailOptions は存在するが、その要素にorderNumber が「1」（メガネ）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の１つ目のボタンに「メガネ」と表示される

#### 正常 未実施 矯正区分：右の examItemDetailOptions.orderNumber が「1」のnameが7文字以下の時
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の１つ目のボタンに examItemDetailOptions.orderNumber が「1」のnameが表示される

#### 正常 未実施 矯正区分：右の examItemDetailOptions.orderNumber が「1」のnameが8文字以上の時
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の１つ目のボタンに examItemDetailOptions.orderNumber が「1」のnameが先頭から7文字表示される

#### 正常 未実施 矯正区分：右の examItemDetailOptions.orderNumber が「2」のnameが空、もしくはundefinedの場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の2つ目のボタンに「コンタクト」と表示される

#### 正常 未実施 矯正区分：右の examItemDetailOptions は存在するが、その要素にorderNumber が「2」（コンタクト）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の2つ目のボタンに「コンタクト」と表示される

#### 正常 未実施 矯正区分：右の examItemDetailOptions.orderNumber が「2」のnameが7文字以下の時
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の2つ目のボタンに examItemDetailOptions.orderNumber が「2」のnameが表示される

#### 正常 未実施 矯正区分：右の examItemDetailOptions.orderNumber が「2」のnameが8文字以上の時
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）の2つ目のボタンに examItemDetailOptions.orderNumber が「2」のnameが先頭から7文字表示される

### 矯正区分　選択ボタン　選択状況


#### 正常 未実施 矯正区分：左の value が examItemDetailOptions.orderNumber が「1」の code と一致する
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）のボタン
* [ ] １つ目：選択
* [ ] ２つ目：未選択

#### 正常 未実施 矯正区分：左の value が examItemDetailOptions.orderNumber が「2」の code と一致する
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）ボタン
* [ ] １つ目：未選択
* [ ] ２つ目：選択

#### 正常 未実施 矯正区分：左の value が examItemDetailOptions.orderNumber が「1」「2」の code と一致しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（左）ボタン
* [ ] １つ目：未選択
* [ ] ２つ目：未選択

#### 正常 未実施 矯正区分：右の value が examItemDetailOptions.orderNumber が「1」の code と一致する
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）のボタン
* [ ] １つ目：選択
* [ ] ２つ目：未選択

#### 正常 未実施 矯正区分：右の value が examItemDetailOptions.orderNumber が「2」の code と一致する
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）ボタン
* [ ] １つ目：未選択
* [ ] ２つ目：選択

#### 正常 未実施 矯正区分：右の value が examItemDetailOptions.orderNumber が「1」「2」の code と一致しない
1. 視力コンポーネントが呼び出される
* [ ] 矯正区分（右）ボタン
* [ ] １つ目：未選択
* [ ] ２つ目：未選択

### 矯正　入力値テキストボックス　Enable


#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「3」（矯正：左）が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の入力値テキストボックスがグレーアウトされる。
* [ ] 矯正（左）の入力値テキストボックスが入力不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「3」（矯正：左）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の入力値テキストボックスがグレーアウトされる。
* [ ] 矯正（左）の入力値テキストボックスが入力不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「3」（矯正：左）が存在する場合（cancelReasonId に値が存在する時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の入力値テキストボックスがグレーアウトされる。
* [ ] 矯正（左）の入力値テキストボックスが入力不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「3」（矯正：左）が存在する場合 （hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の入力値テキストボックスがグレーアウトされず表示される。
* [ ] 矯正（左）の入力値テキストボックスが入力可能。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「4」（矯正：右）が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の入力値テキストボックスがグレーアウトされる。
* [ ] 矯正（右）の入力値テキストボックスが入力不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「4」（矯正：右）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の入力値テキストボックスがグレーアウトされる。
* [ ] 矯正（右）の入力値テキストボックスが入力不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「4」（矯正：右）が存在する場合（cancelReasonId に値が存在する時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の入力値テキストボックスがグレーアウトされる。
* [ ] 矯正（右）の入力値テキストボックスが入力不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「4」（矯正：右）が存在する場合 （hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の入力値テキストボックスがグレーアウトされず表示される。
* [ ] 矯正（右）の入力値テキストボックスが入力可能。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「5」（矯正：両眼）が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の入力値テキストボックスがグレーアウトされる。
* [ ] 矯正（両眼）の入力値テキストボックスが入力不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「5」（矯正：両眼）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の入力値テキストボックスがグレーアウトされる。
* [ ] 矯正（両眼）の入力値テキストボックスが入力不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「5」（矯正：両眼）が存在する場合（cancelReasonId に値が存在する時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の入力値テキストボックスがグレーアウトされる。
* [ ] 矯正（両眼）の入力値テキストボックスが入力不可。

#### 正常 未実施 矯正の　examItemDetails の中に positionNumber が「5」（矯正：両眼）が存在する場合 （hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の入力値テキストボックスがグレーアウトされず表示される。
* [ ] 矯正（両眼）の入力値テキストボックスが入力可能。


### 矯正　入力値テキストボックス　カラー


#### 正常 未実施 矯正にエラーがない場合。
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左・右・両眼）の入力値テキストボックスがデフォルトカラーで表示される

#### 正常 未実施 矯正にエラーがある場合。　矯正の examRegistResults に含まれる errorLevel の最大値が 3 （エラー）の場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左・右・両眼）の入力値テキストボックスがテーマ"error"の色で表示される。

#### 正常 未実施 矯正にエラーがある場合。　矯正の examRegistResults に含まれる errorLevel の最大値が 2 （ワーニング）の場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左・右・両眼）の入力値テキストボックスがテーマ"warning"の色で表示される。


### 矯正　入力値テキストボックス　値


#### 正常 未実施 以降のテストは、引数.検査項目情報の examItems の positionNumber が「1」（矯正）の要素の hasOrder = true かつ cancelReasonId に値が存在しない条件で行う↓

#### 正常 未実施 矯正（左）の値が空の場合。　矯正の examItemDetails の中の  positionNumber が「3」の要素（矯正：左）の value が数値以外の場合（空）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 矯正（左）の値が不正な場合。　矯正の examItemDetails の中の  positionNumber が「3」の要素（矯正：左）の value が数値以外の場合（undefined）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 矯正（右）の値が空の場合。　矯正の examItemDetails の中の  positionNumber が「4」の要素（矯正：右）の value が数値以外の場合（空）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 矯正（右）の値が不正な場合。　矯正の examItemDetails の中の  positionNumber が「4」の要素（矯正：右）の value が数値以外の場合（undefined）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 矯正（両眼）の値が空の場合。　矯正の examItemDetails の中の  positionNumber が「5」の要素（矯正：両眼）の value が数値以外の場合（空）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 矯正（両眼）の値が不正な場合。　矯正の examItemDetails の中の  positionNumber が「5」の要素（矯正：両眼）の value が数値以外の場合（undefined）
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の入力値テキストボックスが空欄で表示される。

#### 正常 未実施 矯正（左）の値が数値の場合。　矯正の examItemDetails の中の  positionNumber が「3」の要素（矯正：左）の value が数値の場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の入力値テキストボックスに value の値が表示される。

#### 正常 未実施 矯正（右）の値が数値の場合。　矯正の examItemDetails の中の  positionNumber が「4」の要素（矯正：右）の value が数値の場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の入力値テキストボックスに value の値が表示される。

#### 正常 未実施 矯正（両眼）の値が数値の場合。　矯正の examItemDetails の中の  positionNumber が「5」の要素（矯正：両眼）の value が数値の場合
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の入力値テキストボックスに value の値が表示される。


### 特記　選択ボタン　Enable


#### 正常 未実施 引数.検査項目情報の examItems にpositionNumber が「3」（特記）の要素が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 検査項目：特記の選択ボタンが全てグレーアウトされる。
* [ ] 選択ボタンは選択不可。

#### 正常 未実施 以降のテストは、引数.検査項目情報の examItems にpositionNumber が「3」（特記）の要素が存在する条件で行う↓

#### 正常 未実施 特記の examItemDetails が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 検査項目：特記の選択ボタンが全てグレーアウトされる。
* [ ] 選択ボタンは選択不可。

#### 正常 未実施 特記の　examItemDetails の中に positionNumber が「1」（特記：左）が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の選択ボタンがグレーアウトされる。
* [ ] 特記（左）の選択ボタンは選択不可。

#### 正常 未実施 特記の　examItemDetails の中に positionNumber が「1」（特記：左）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の選択ボタンがグレーアウトされる。
* [ ] 特記（左）の選択ボタンは選択不可。

#### 正常 未実施 特記の　examItemDetails の中に positionNumber が「1」（特記：左）が存在する場合（cancelReasonId に値が存在 する時）
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の選択ボタンがグレーアウトされる。
* [ ] 特記（左）の選択ボタンは選択不可。

#### 正常 未実施 特記の　examItemDetails の中に positionNumber が「1」（特記：左）が存在する場合（hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の選択ボタンがグレーアウトされずに表示される。
* [ ] 特記（左）の選択ボタンは選択可能。
- ※ボタンごとのグレーアウトテストは以降で行う。

#### 正常 未実施 以降のテストは、特記の examItemsDetails の positionNumber が「1」（特記：左）の要素の hasOrder = true かつ cancelReasonId に値が存在しない条件で行う↓　

#### 異常 未実施 特記：左の　examItemDetailOptions が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の選択ボタンがグレーアウトされる。
* [ ] 特記（左）の選択ボタンは選択不可。

#### 異常 未実施 特記：左の　examItemDetailOptions は存在するが、その要素が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の選択ボタンがグレーアウトされる。
* [ ] 特記（左）の選択ボタンは選択不可。

#### 正常 未実施 特記：左の examItemDetailOptions は存在するが、その要素にorderNumber が「1」（メガネ不要）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の選択ボタンの１つ目（メガネ不要）がグレーアウトされる。
* [ ] 特記（左）の選択ボタン１つ目は選択不可。

#### 正常 未実施 特記：左の examItemDetailOptions は存在するが、その要素にorderNumber が「2」（コンタクト不要）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の選択ボタンの２つ目（コンタクト不要）がグレーアウトされる。
* [ ] 特記（左）の選択ボタン２つ目は選択不可。
- ※優先度低。

#### 正常 未実施 特記：左の examItemDetailOptions は存在するが、その要素にorderNumber が「1」「2」が存在する
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の選択ボタンの１つ目と２つ目がグレーアウトされずに表示される。
* [ ] 特記（左）の選択ボタンは選択可能。

#### 正常 未実施 特記の　examItemDetails の中に positionNumber が「2」（特記：右）が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の選択ボタンがグレーアウトされる。
* [ ] 特記（右）の選択ボタンは選択不可。

#### 正常 未実施 特記の　examItemDetails の中に positionNumber が「2」（特記：右）が存在する場合（hasOrder = false の時）
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の選択ボタンがグレーアウトされる。
* [ ] 特記（右）の選択ボタンは選択不可。

#### 正常 未実施 特記の　examItemDetails の中に positionNumber が「2」（特記：右）が存在する場合（cancelReasonId に値が存在 する時）
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の選択ボタンがグレーアウトされる。
* [ ] 特記（右）の選択ボタンは選択不可。

#### 正常 未実施 特記の　examItemDetails の中に positionNumber が「2」（特記：右）が存在する場合（hasOrder = true かつ cancelReasonId に値が存在しない時）
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の選択ボタンがグレーアウトされずに表示される。
* [ ] 特記（右）の選択ボタンは選択可能。
- ※ボタンごとのグレーアウトテストは以降で行う。

#### 正常 未実施 以降のテストは、特記の examItemsDetails の positionNumber が「2」（特記：右）の要素の hasOrder = true かつ cancelReasonId に値が存在しない条件で行う↓　

#### 異常 未実施 特記：右の　examItemDetailOptions が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の選択ボタンがグレーアウトされる。
* [ ] 特記（右）の選択ボタンは選択不可。

#### 異常 未実施 特記：右の　examItemDetailOptions は存在するが、その要素が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の選択ボタンがグレーアウトされる。
* [ ] 特記（右）の選択ボタンは選択不可。

#### 正常 未実施 特記：右の examItemDetailOptions は存在するが、その要素にorderNumber が「1」（メガネ不要）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の選択ボタンの１つ目（メガネ不要）がグレーアウトされる。
* [ ] 特記（右）の選択ボタン１つ目は選択不可。

#### 正常 未実施 特記：右の examItemDetailOptions は存在するが、その要素にorderNumber が「2」（コンタクト不要）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の選択ボタンの２つ目（コンタクト不要）がグレーアウトされる。
* [ ] 特記（右）の選択ボタン２つ目は選択不可。
- ※優先度低。

#### 正常 未実施 特記：右の examItemDetailOptions は存在するが、その要素にorderNumber が「1」「2」存在する
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の選択ボタンの１つ目と２つ目がグレーアウトされずに表示される。
* [ ] 特記（右）の選択ボタンは選択可能。


### 特記　選択ボタン　値


#### 正常 未実施 特記：左の examItemDetailOptions.orderNumber が「1」のnameが空、もしくはundefinedの場合
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の１つ目のボタンに「メガネ不要」と表示される。

#### 正常 未実施 特記：左の examItemDetailOptions は存在するが、その要素にorderNumber が「1」（メガネ不要）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の１つ目のボタンに「メガネ不要」と表示される。

#### 正常 未実施 特記：左の examItemDetailOptions.orderNumber が「1」のnameが7文字以下の時
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の１つ目のボタンに examItemDetailOptions.orderNumber が「1」のnameが表示される。

#### 正常 未実施 特記：左の examItemDetailOptions.orderNumber が「1」のnameが8文字以上の時
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の１つ目のボタンに examItemDetailOptions.orderNumber が「1」のnameが先頭から7文字表示される。

#### 正常 未実施 特記：左の examItemDetailOptions.orderNumber が「2」のnameが空、もしくはundefinedの場合
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の2つ目のボタンに「コンタクト不要」と表示される。

#### 正常 未実施 特記：左の examItemDetailOptions は存在するが、その要素にorderNumber が「2」（コンタクト不要）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の2つ目のボタンに「コンタクト不要」と表示される。

#### 正常 未実施 特記：左の examItemDetailOptions.orderNumber が「2」のnameが7文字以下の時
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の2つ目のボタンに examItemDetailOptions.orderNumber が「2」のnameが表示される。

#### 正常 未実施 特記：左の examItemDetailOptions.orderNumber が「2」のnameが8文字以上の時
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の2つ目のボタンに examItemDetailOptions.orderNumber が「2」のnameが先頭から7文字表示される。

#### 正常 未実施 特記：右の examItemDetailOptions.orderNumber が「1」のnameが空、もしくはundefinedの場合
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の１つ目のボタンに「メガネ不要」と表示される。

#### 正常 未実施 特記：右の examItemDetailOptions は存在するが、その要素にorderNumber が「1」（メガネ不要）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の１つ目のボタンに「メガネ不要」と表示される。

#### 正常 未実施 特記：右の examItemDetailOptions.orderNumber が「1」のnameが7文字以下の時
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の１つ目のボタンに examItemDetailOptions.orderNumber が「1」のnameが表示される。

#### 正常 未実施 特記：右の examItemDetailOptions.orderNumber が「1」のnameが8文字以上の時
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の１つ目のボタンに examItemDetailOptions.orderNumber が「1」のnameが先頭から7文字表示される。

#### 正常 未実施 特記：右の examItemDetailOptions.orderNumber が「2」のnameが空、もしくはundefinedの場合
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の2つ目のボタンに「コンタクト不要」と表示される。

#### 正常 未実施 特記：右の examItemDetailOptions は存在するが、その要素にorderNumber が「2」（コンタクト不要）が存在しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の2つ目のボタンに「コンタクト不要」と表示される。

#### 正常 未実施 特記：右の examItemDetailOptions.orderNumber が「2」のnameが7文字以下の時
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の2つ目のボタンに examItemDetailOptions.orderNumber が「2」のnameが表示される。

#### 正常 未実施 特記：右の examItemDetailOptions.orderNumber が「2」のnameが8文字以上の時
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の2つ目のボタンに examItemDetailOptions.orderNumber が「2」のnameが先頭から7文字表示される。


### 特記　選択ボタン　選択状況


#### 正常 未実施 特記：左の value が examItemDetailOptions.orderNumber が「1」の code と一致する
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）のボタン
* [ ] １つ目：選択
* [ ] ２つ目：未選択

#### 正常 未実施 特記：左の value が examItemDetailOptions.orderNumber が「2」の code と一致する
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）ボタン
* [ ] １つ目：未選択
* [ ] ２つ目：選択

#### 正常 未実施 特記：左の value が examItemDetailOptions.orderNumber が「1」「2」の code と一致しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）ボタン
* [ ] １つ目：未選択
* [ ] ２つ目：未選択

#### 正常 未実施 特記：右の value が examItemDetailOptions.orderNumber が「1」の code と一致する
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）のボタン
* [ ] １つ目：選択
* [ ] ２つ目：未選択

#### 正常 未実施 特記：右の value が examItemDetailOptions.orderNumber が「2」の code と一致する
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）ボタン
* [ ] １つ目：未選択
* [ ] ２つ目：選択

#### 正常 未実施 特記：右の value が examItemDetailOptions.orderNumber が「1」「2」の code と一致しない
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）ボタン
* [ ] １つ目：未選択
* [ ] ２つ目：未選択


### 前回値


#### 正常 未実施 裸眼：左の、 examItemDetail.positionNumberが「1」のprevValue が存在しない時 
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（左）の前回値は非表示。

#### 正常 未実施 裸眼：左の、 examItemDetail.positionNumberが「1」のprevValue の値が空、もしくはundefindの時
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（左）の前回値は非表示。

#### 正常 未実施 裸眼：左の、 examItemDetail.positionNumberが「1」のprevValue の値がある時
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（左）の前回値は表示される。
* [ ] （前回値：{prevValue}）の表示。

#### 正常 未実施 裸眼：右の、 examItemDetail.positionNumberが「1」のprevValue が存在しない時 
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（右）の前回値は非表示。

#### 正常 未実施 裸眼：右の、 examItemDetail.positionNumberが「1」のprevValue の値が空、もしくはundefindの時
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（右）の前回値は非表示。

#### 正常 未実施 裸眼：右の、 examItemDetail.positionNumberが「1」のprevValue の値がある時
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（右）の前回値は表示される。
* [ ] （前回値：{prevValue}）の表示。

#### 正常 未実施 裸眼：両眼の、 examItemDetail.positionNumberが「1」のprevValue が存在しない時 
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（両眼）の前回値は非表示。

#### 正常 未実施 裸眼：両眼の、 examItemDetail.positionNumberが「1」のprevValue の値が空、もしくはundefindの時
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（両眼）の前回値は非表示。

#### 正常 未実施 裸眼：両眼の、 examItemDetail.positionNumberが「1」のprevValue の値がある時
1. 視力コンポーネントが呼び出される
* [ ] 裸眼（両眼）の前回値は表示される。
* [ ] （前回値：{prevValue}）の表示。

#### 正常 未実施 矯正区分：左のprevValue が存在する時 
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の前回値は非表示。

#### 正常 未実施 矯正区分：右のprevValue が存在する時 
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の前回値は非表示。

#### 正常 未実施 矯正：左の、 examItemDetail.positionNumberが「1」のprevValue が存在しない時 
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の前回値は非表示。

#### 正常 未実施 矯正：左の、 examItemDetail.positionNumberが「1」のprevValue の値が空、もしくはundefindの時
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の前回値は非表示。

#### 正常 未実施 矯正：左の、 examItemDetail.positionNumberが「1」のprevValue の値がある時
1. 視力コンポーネントが呼び出される
* [ ] 矯正（左）の前回値は表示される。
* [ ] （前回値：{prevValue}）の表示。

#### 正常 未実施 矯正：右の、 examItemDetail.positionNumberが「1」のprevValue が存在しない時 
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の前回値は非表示。

#### 正常 未実施 矯正：右の、 examItemDetail.positionNumberが「1」のprevValue の値が空、もしくはundefindの時
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の前回値は非表示。

#### 正常 未実施 矯正：右の、 examItemDetail.positionNumberが「1」のprevValue の値がある時
1. 視力コンポーネントが呼び出される
* [ ] 矯正（右）の前回値は表示される。
* [ ] （前回値：{prevValue}）の表示。

#### 正常 未実施 矯正：両眼の、 examItemDetail.positionNumberが「1」のprevValue が存在しない時 
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の前回値は非表示。

#### 正常 未実施 矯正：両眼の、 examItemDetail.positionNumberが「1」のprevValue の値が空、もしくはundefindの時
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の前回値は非表示。

#### 正常 未実施 矯正：両眼の、 examItemDetail.positionNumberが「1」のprevValue の値がある時
1. 視力コンポーネントが呼び出される
* [ ] 矯正（両眼）の前回値は表示される。
* [ ] （前回値：{prevValue}）の表示。

#### 正常 未実施 特記：左のprevValue が存在する時 
1. 視力コンポーネントが呼び出される
* [ ] 特記（左）の前回値は非表示。

#### 正常 未実施 特記：右のprevValue が存在する時 
1. 視力コンポーネントが呼び出される
* [ ] 特記（右）の前回値は非表示。


### 単位


#### 正常 未実施 裸眼：左
1. 視力コンポーネントが呼び出される
* [ ] 表示なし

#### 正常 未実施 裸眼：右
1. 視力コンポーネントが呼び出される
* [ ] 表示なし

#### 正常 未実施 裸眼：両眼
1. 視力コンポーネントが呼び出される
* [ ] 表示なし

#### 正常 未実施 矯正区分：左
1. 視力コンポーネントが呼び出される
* [ ] 表示なし

#### 正常 未実施 矯正区分：右
1. 視力コンポーネントが呼び出される
* [ ] 表示なし

#### 正常 未実施 矯正：左
1. 視力コンポーネントが呼び出される
* [ ] 表示なし

#### 正常 未実施 矯正：右
1. 視力コンポーネントが呼び出される
* [ ] 表示なし

#### 正常 未実施 矯正：両眼
1. 視力コンポーネントが呼び出される
* [ ] 表示なし

#### 正常 未実施 特記：左
1. 視力コンポーネントが呼び出される
* [ ] 表示なし

#### 正常 未実施 特記：右
1. 視力コンポーネントが呼び出される
* [ ] 表示なし


### エラーメッセージ 

#### 正常 未実施 裸眼の examRegistResults に値が設定されていない or undefined の場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 裸眼の examRegistResults にエラーレベル：3（異常） の値が設定されている場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 裸眼の examRegistResults にエラーレベル：2（警告） の値が設定されている場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示される。
* [ ] アイコン：！
* [ ] 文字色：warning

#### 正常 未実施 裸眼　必須チェック
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。
- ※初期表示は必須チェックなし。

#### 正常 未実施 裸眼　基準値チェックに該当する場合
1. 視力コンポーネントが呼び出される
* [ ] 基準値チェックのエラーメッセージが表示される。
- ※詳細なチェックは共通関数が行うため省略。

#### 正常 未実施 裸眼の examRegistResults に含まれる要素が複数ある場合。
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが複数行表示される。

#### 正常 未実施 矯正の examRegistResults に値が設定されていない or undefined の場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 矯正の examRegistResults にエラーレベル：3（異常） の値が設定されている場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 矯正の examRegistResults にエラーレベル：2（警告） の値が設定されている場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示される。
* [ ] アイコン：！
* [ ] 文字色：warning

#### 正常 未実施 矯正　必須チェック
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 矯正　基準値チェックに該当する場合
1. 視力コンポーネントが呼び出される
* [ ] 基準値チェックのエラーメッセージが表示される。
- ※詳細なチェックは共通関数が行うため省略。

#### 正常 未実施 矯正の examRegistResults に含まれる要素が複数ある場合。
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが複数行表示される。

#### 正常 未実施 視力矯正の固有チェック　矯正区分：左と矯正：左の examItemDetails の要素が存在し、両方の hasOrder=true かつ cancelReasonId に値が存在しない時、矯正区分：左の value に値があるが、矯正値：左の value に値が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージ「矯正区分（左）が選択されていますが、矯正値（左）が入力されていません。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 視力矯正の固有チェック　矯正区分：左と矯正：左の examItemDetails の要素が存在し、両方の hasOrder=true かつ cancelReasonId に値が存在しない時、矯正区分：左の value には値がないが、矯正値：左の value に値が存在する場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージ「矯正値（左）が入力されていますが、矯正区分（左）が選択されていません。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 視力矯正の固有チェック　矯正の examItem の要素が存在し、矯正区分：左と矯正：左の examItemDetails の要素が存在するが、一方でも　hasOrder=false の時、矯正区分：左の value に値があるが、矯正値：左の value に値が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 視力矯正の固有チェック　矯正の examItem の要素が存在し、矯正区分：左と矯正：左の examItemDetails の要素が存在するが、一方でも　hasOrder=false の時、矯正区分：左の value には値がないが、矯正値：左の value に値が存在する場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 視力矯正の固有チェック　矯正の examItem の要素が存在し、矯正区分：左と矯正：左の examItemDetails の要素が存在するが、一方でも　cancelReasonId に値がある時、矯正区分：左の value に値があるが、矯正値：左の value に値が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 視力矯正の固有チェック　矯正の examItem の要素が存在し、矯正区分：左と矯正：左の examItemDetails の要素が存在するが、一方でも　cancelReasonId に値がある時、矯正区分：左の value には値がないが、矯正値：左の value に値が存在する場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 視力矯正の固有チェック　矯正区分：右と矯正：右の examItemDetails の要素が存在し、両方の hasOrder=true かつ cancelReasonId に値が存在しない時、矯正区分：右の value に値があるが、矯正値：右の value に値が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージ「矯正区分（右）が選択されていますが、矯正値（右）が入力されていません。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 視力矯正の固有チェック　矯正区分：右と矯正：右の examItemDetails の要素が存在し、両方の hasOrder=true かつ cancelReasonId に値が存在しない時、矯正区分：右の value には値がないが、矯正値：右の value に値が存在する場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージ「矯正値（右）が入力されていますが、矯正区分（右）が選択されていません。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 視力矯正の固有チェック　矯正の examItem の要素が存在し、矯正区分：右と矯正：右の examItemDetails の要素が存在するが、一方でも　hasOrder=false の時、矯正区分：右の value に値があるが、矯正値：右の value に値が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 視力矯正の固有チェック　矯正の examItem の要素が存在し、矯正区分：右と矯正：右の examItemDetails の要素が存在するが、一方でも　hasOrder=false の時、矯正区分：右の value には値がないが、矯正値：右の value に値が存在する場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 視力矯正の固有チェック　矯正の examItem の要素が存在し、矯正区分：右と矯正：右の examItemDetails の要素が存在するが、一方でも　cancelReasonId に値がある時、矯正区分：右の value に値があるが、矯正値：右の value に値が存在しない場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 視力矯正の固有チェック　矯正の examItem の要素が存在し、矯正区分：右と矯正：右の examItemDetails の要素が存在するが、一方でも　cancelReasonId に値がある時、矯正区分：右の value には値がないが、矯正値：右の value に値が存在する場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 矯正の examRegistResults に要素があり、かつ視力矯正の固有チェックのエラーメッセージも存在する場合
1. 視力コンポーネントが呼び出される
* [ ]  examRegistResults のエラーメッセージと固有チェックのエラーメッセージが両方表示される。

#### 正常 未実施 特記の examRegistResults に値が設定されていない or undefined の場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。

#### 正常 未実施 特記の examRegistResults にエラーレベル：3（異常） の値が設定されている場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 特記の examRegistResults にエラーレベル：2（警告） の値が設定されている場合
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示される。
* [ ] アイコン：！
* [ ] 文字色：warning

#### 正常 未実施 特記　必須チェック
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが表示されていない。
- ※初期表示は必須チェックなし。

#### 正常 未実施 特記の examRegistResults に含まれる要素が複数ある場合。
1. 視力コンポーネントが呼び出される
* [ ] エラーメッセージが複数行表示される。


## 入力値テキストボックスの押下
### キーボード（整数）の表示


#### 正常 未実施 裸眼：左　グレーアウトしてない状態　キーボードタイプ：１（整数）　
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
* [ ] キーボード（整数）が表示される。

#### 正常 未実施 裸眼：右　グレーアウトしてない状態　キーボードタイプ：１（整数）
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスをタップ
* [ ] キーボード（整数）が表示される。

#### 正常 未実施 裸眼：両眼　グレーアウトしてない状態　キーボードタイプ：１（整数）
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
* [ ] キーボード（整数）が表示される。

#### 正常 未実施 裸眼：左　グレーアウト状態　キーボードタイプ：１（整数）　
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
* [ ] キーボード（整数）が表示されない。

#### 正常 未実施 裸眼：右　グレーアウト状態　キーボードタイプ：１（整数）
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスをタップ
* [ ] キーボード（整数）が表示されない。

#### 正常 未実施 裸眼：両眼　グレーアウト状態　キーボードタイプ：１（整数）
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
* [ ] キーボード（整数）が表示されない。


### キーボード（選択）の表示


#### 正常 未実施 裸眼：左　グレーアウトしてない状態　キーボードタイプ：２（選択）　
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
* [ ] キーボード（選択）が表示される。

#### 正常 未実施 裸眼：右　グレーアウトしてない状態　キーボードタイプ：２（選択）
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスをタップ
* [ ] キーボード（選択）が表示される。

#### 正常 未実施 裸眼：両眼　グレーアウトしてない状態　キーボードタイプ：２（選択）
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
* [ ] キーボード（選択）が表示される。

#### 正常 未実施 裸眼：左　グレーアウト状態　キーボードタイプ：２（選択）　
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
* [ ] キーボード（選択）が表示されない。

#### 正常 未実施 裸眼：右　グレーアウト状態　キーボードタイプ：２（選択）
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスをタップ
* [ ] キーボード（選択）が表示されない。

#### 正常 未実施 裸眼：両眼　グレーアウト状態　キーボードタイプ：２（選択）
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
* [ ] キーボード（選択）が表示されない。


### 他のキーボードが非表示になる確認


#### 正常 未実施 裸眼：左のキーボードが表示されている状態で、裸眼：右のテキストボックスをタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
3. 裸眼（左）のキーボードが表示される
4. 裸眼（右）の入力値テキストボックスをタップ
* [ ] 裸眼（左）のキーボードが非表示になる。
* [ ] 裸眼（右）のキーボードが表示される。

#### 正常 未実施 矯正：右のキーボードが表示されている状態で、裸眼：右のテキストボックスをタップ
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスをタップ
3. 矯正（右）のキーボードが表示される
4. 裸眼（右）の入力値テキストボックスをタップ
* [ ] 矯正（右）のキーボードが非表示になる。
* [ ] 裸眼（右）のキーボードが表示される。

#### 正常 未実施 矯正：両眼のキーボードが表示されている状態で、矯正区分：左の選択肢ボタンをタップ
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスをタップ
3. 矯正（両眼）のキーボードが表示される
4. 矯正区分（左）の選択肢ボタンをタップ
* [ ] 矯正（両眼）のキーボードが非表示になる。
* [ ] 矯正区分（左）の選択肢ボタンが選択される。

#### 正常 未実施 裸眼：両眼のキーボードが表示されている状態で、特記：右の選択肢ボタンをタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
3. 裸眼（両眼）のキーボードが表示される
4. 特記（右）の選択肢ボタンをタップ
* [ ] 裸眼（両眼）のキーボードが非表示になる。
* [ ] 特記（右）の選択肢ボタンが選択される。

## キーボード（整数）入力
### 入力値テキストボックスの値変化


#### 正常 未実施 裸眼：左　数字ボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
3. 裸眼（左）のキーボードが表示される
4. 数字ボタンをタップ
* [ ] タップした数字が裸眼（左）の入力値テキストボックスに表示される。

#### 正常 未実施 裸眼：左　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
3. 裸眼（左）のキーボードが表示される
4. ACボタンをタップ
* [ ] 裸眼（左）の入力値テキストボックスがクリアされる。

#### 正常 未実施 裸眼：左　確定ボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
3. 裸眼（左）のキーボードが表示される
4. 数字ボタンをタップし、値を入力
5. 確定ボタンタップ
* [ ] 裸眼（左）のキーボードが非表示になる。

#### 正常 未実施 裸眼：右　数字ボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスをタップ
3. 裸眼（右）のキーボードが表示される
4. 数字ボタンをタップ
* [ ] タップした数字が裸眼（右）の入力値テキストボックスに表示される。

#### 正常 未実施 裸眼：右　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスをタップ
3. 裸眼（右）のキーボードが表示される
4. ACボタンをタップ
* [ ] 裸眼（右）の入力値テキストボックスがクリアされる。

#### 正常 未実施 裸眼：右　確定ボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスをタップ
3. 裸眼（右）のキーボードが表示される
4. 数字ボタンをタップし、値を入力
5. 確定ボタンタップ
* [ ] 裸眼（右）のキーボードが非表示になる。

#### 正常 未実施 裸眼：両眼　数字ボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
3. 裸眼（両眼）のキーボードが表示される
4. 数字ボタンをタップ
* [ ] タップした数字が裸眼（両眼）の入力値テキストボックスに表示される。

#### 正常 未実施 裸眼：両眼　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
3. 裸眼（両眼）のキーボードが表示される
4. ACボタンをタップ
* [ ] 裸眼（両眼）の入力値テキストボックスがクリアされる。

#### 正常 未実施 裸眼：両眼　確定ボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
3. 裸眼（両眼）のキーボードが表示される
4. 数字ボタンをタップし、値を入力
5. 確定ボタンタップ
* [ ] 裸眼（両眼）のキーボードが非表示になる。

#### 正常 未実施 矯正：左　数字ボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスをタップ
3. 矯正（左）のキーボードが表示される
4. 数字ボタンをタップ
* [ ] タップした数字が矯正（左）の入力値テキストボックスに表示される。

#### 正常 未実施 矯正：左　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスをタップ
3. 矯正（左）のキーボードが表示される
4. ACボタンをタップ
* [ ] 矯正（左）の入力値テキストボックスがクリアされる。

#### 正常 未実施 矯正：左　確定ボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスをタップ
3. 矯正（左）のキーボードが表示される
4. 数字ボタンをタップし、値を入力
5. 確定ボタンタップ
* [ ] 矯正（左）のキーボードが非表示になる。

#### 正常 未実施 矯正：右　数字ボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスをタップ
3. 矯正（右）のキーボードが表示される
4. 数字ボタンをタップ
* [ ] タップした数字が矯正（右）の入力値テキストボックスに表示される。

#### 正常 未実施 矯正：右　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスをタップ
3. 矯正（右）のキーボードが表示される
4. ACボタンをタップ
* [ ] 矯正（右）の入力値テキストボックスがクリアされる。

#### 正常 未実施 矯正：右　確定ボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスをタップ
3. 矯正（右）のキーボードが表示される
4. 数字ボタンをタップし、値を入力
5. 確定ボタンタップ
* [ ] 矯正（右）のキーボードが非表示になる。

#### 正常 未実施 矯正：両眼　数字ボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスをタップ
3. 矯正（両眼）のキーボードが表示される
4. 数字ボタンをタップ
* [ ] タップした数字が矯正（両眼）の入力値テキストボックスに表示される。

#### 正常 未実施 矯正：両眼　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスをタップ
3. 矯正（両眼）のキーボードが表示される
4. ACボタンをタップ
* [ ] 矯正（両眼）の入力値テキストボックスがクリアされる。

#### 正常 未実施 矯正：両眼　確定ボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスをタップ
3. 矯正（両眼）のキーボードが表示される
4. 数字ボタンをタップし、値を入力
5. 確定ボタンタップ
* [ ] 矯正（両眼）のキーボードが非表示になる。


## キーボード（選択）入力
### 入力値テキストボックスの値変化


#### 正常 未実施 裸眼：左　カスタムボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
3. 裸眼（左）のキーボードが表示される
4. カスタムボタンをタップ
* [ ] タップしたカスタムボタンの値が裸眼（左）の入力値テキストボックスに表示される。

#### 正常 未実施 裸眼：左　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスをタップ
3. 裸眼（左）のキーボードが表示される
4. ACボタンをタップ
* [ ] 裸眼（左）の入力値テキストボックスがクリアされる。

#### 正常 未実施 裸眼：右　カスタムボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスをタップ
3. 裸眼（右）のキーボードが表示される
4. カスタムボタンをタップ
* [ ] タップしたカスタムボタンの値が裸眼（右）の入力値テキストボックスに表示される。

#### 正常 未実施 裸眼：右　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスをタップ
3. 裸眼（右）のキーボードが表示される
4. ACボタンをタップ
* [ ] 裸眼（右）の入力値テキストボックスがクリアされる。

#### 正常 未実施 裸眼：両眼　カスタムボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
3. 裸眼（両眼）のキーボードが表示される
4. カスタムボタンをタップ
* [ ] タップしたカスタムボタンの値が裸眼（両眼）の入力値テキストボックスに表示される。

#### 正常 未実施 裸眼：両眼　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスをタップ
3. 裸眼（両眼）のキーボードが表示される
4. ACボタンをタップ
* [ ] 裸眼（両眼）の入力値テキストボックスがクリアされる。

#### 正常 未実施 矯正：左　カスタムボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスをタップ
3. 矯正（左）のキーボードが表示される
4. カスタムボタンをタップ
* [ ] タップしたカスタムボタンの値が矯正（左）の入力値テキストボックスに表示される。

#### 正常 未実施 矯正：左　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスをタップ
3. 矯正（左）のキーボードが表示される
4. ACボタンをタップ
* [ ] 矯正（左）の入力値テキストボックスがクリアされる。

#### 正常 未実施 矯正：右　カスタムボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスをタップ
3. 矯正（右）のキーボードが表示される
4. カスタムボタンをタップ
* [ ] タップしたカスタムボタンの値が矯正（右）の入力値テキストボックスに表示される。

#### 正常 未実施 矯正：右　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスをタップ
3. 矯正（右）のキーボードが表示される
4. ACボタンをタップ
* [ ] 矯正（右）の入力値テキストボックスがクリアされる。

#### 正常 未実施 矯正：両眼　カスタムボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスをタップ
3. 矯正（両眼）のキーボードが表示される
4. カスタムボタンをタップ
* [ ] タップしたカスタムボタンの値が矯正（両眼）の入力値テキストボックスに表示される。

#### 正常 未実施 矯正：両眼　ACボタンタップ
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスをタップ
3. 矯正（両眼）のキーボードが表示される
4. ACボタンをタップ
* [ ] 矯正（両眼）の入力値テキストボックスがクリアされる。


## 選択肢の押下
### 選択項目の表示の変化


#### 正常 未実施 矯正：左の未選択の選択肢ボタンをタップ（矯正：左の選択中ボタンなしの状態）
1. 視力コンポーネントが呼び出される
2. 矯正（左）の選択肢ボタンをタップ
* [ ] 選択したボタンが選択状態になる。

#### 正常 未実施 矯正：左の未選択の選択肢ボタンをタップ（矯正：左の選択中ボタンありの状態）
1. 視力コンポーネントが呼び出される
2. 矯正（左）の選択肢ボタンをタップ
* [ ] 選択したボタンが選択状態になる。
* [ ] 元々選択されていたボタンが未選択状態になる。

#### 正常 未実施 矯正：左の選択中の選択肢ボタンをタップ
1. 視力コンポーネントが呼び出される
2. 矯正（左）の選択肢ボタンをタップ
3. ↑と同じ選択肢ボタンをタップ
* [ ] 選択されていたボタンが未選択状態になる。

#### 正常 未実施 矯正：右の未選択の選択肢ボタンをタップ（矯正：右の選択中ボタンなしの状態）
1. 視力コンポーネントが呼び出される
2. 矯正（右）の選択肢ボタンをタップ
* [ ] 選択したボタンが選択状態になる。

#### 正常 未実施 矯正：右の未選択の選択肢ボタンをタップ（矯正：右の選択中ボタンありの状態）
1. 視力コンポーネントが呼び出される
2. 矯正（右）の選択肢ボタンをタップ
* [ ] 選択したボタンが選択状態になる。
* [ ] 元々選択されていたボタンが未選択状態になる。

#### 正常 未実施 矯正：右の選択中の選択肢ボタンをタップ
1. 視力コンポーネントが呼び出される
2. 矯正（右）の選択肢ボタンをタップ
3. ↑と同じ選択肢ボタンをタップ
* [ ] 選択されていたボタンが未選択状態になる。

#### 正常 未実施 特記：左の未選択の選択肢ボタンをタップ（特記：左の選択中ボタンなしの状態）
1. 視力コンポーネントが呼び出される
2. 特記（左）の選択肢ボタンをタップ
* [ ] 選択したボタンが選択状態になる。

#### 正常 未実施 特記：左の未選択の選択肢ボタンをタップ（特記：左の選択中ボタンありの状態）
1. 視力コンポーネントが呼び出される
2. 特記（左）の選択肢ボタンをタップ
* [ ] 選択したボタンが選択状態になる。
* [ ] 元々選択されていたボタンが未選択状態になる。

#### 正常 未実施 特記：左の選択中の選択肢ボタンをタップ
1. 視力コンポーネントが呼び出される
2. 特記（左）の選択肢ボタンをタップ
3. ↑と同じ選択肢ボタンをタップ
* [ ] 選択されていたボタンが未選択状態になる。

#### 正常 未実施 特記：右の未選択の選択肢ボタンをタップ（特記：右の選択中ボタンなしの状態）
1. 視力コンポーネントが呼び出される
2. 特記（右）の選択肢ボタンをタップ
* [ ] 選択したボタンが選択状態になる。

#### 正常 未実施 特記：右の未選択の選択肢ボタンをタップ（特記：右の選択中ボタンありの状態）
1. 視力コンポーネントが呼び出される
2. 特記（右）の選択肢ボタンをタップ
* [ ] 選択したボタンが選択状態になる。
* [ ] 元々選択されていたボタンが未選択状態になる。

#### 正常 未実施 特記：右の選択中の選択肢ボタンをタップ
1. 視力コンポーネントが呼び出される
2. 特記（右）の選択肢ボタンをタップ
3. ↑と同じ選択肢ボタンをタップ
* [ ] 選択されていたボタンが未選択状態になる。


## onRegisterPressed が true になった時
### エラーメッセージ（必須）


#### 正常 未実施 必須チェック　視力コンポーネントの全ての検査項目オーダーがあり、すべての検査項目明細の hasOrder = true かつ cancelReasonId の値がない状態で、すべての検査項目明細が未検査の場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] エラーメッセージ「視力は入力必須項目です。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 必須チェック　裸眼：両眼の検査項目明細のみ、 value に値があるが、hasOrder = false になっており、その他の検査項目明細は hasOrder = true かつ cancelReasonId の値がない状態で、 value がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 必須チェックのエラーメッセージは表示されない。

#### 正常 未実施 必須チェック　矯正区分：右の検査項目明細のみ、 value に値があるが、hasOrder = false になっており、その他の検査項目明細は hasOrder = true かつ cancelReasonId の値がない状態で、 value がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 必須チェックのエラーメッセージは表示されない。


### エラーメッセージ（固有）


#### 正常 未実施 以降のテストは、矯正区分：左と、矯正：左の  hasOrder = true かつ cancelReasonId の値がない状態でテストする↓

#### 正常 未実施 固有チェック　矯正区分：左と、矯正：左の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：左と、矯正：左の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：左の value は値があり、矯正：左の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] エラーメッセージ「矯正区分（左）が選択されていますが、矯正値（左）が入力されていません。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 固有チェック　矯正区分：左の value に値がなく、矯正：左の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] エラーメッセージ「矯正値（左）が入力されていますが、矯正区分（左）が選択されていません。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 以降のテストは、矯正区分：左の hasOrder = false で、矯正：左の  hasOrder = true かつ cancelReasonId の値がない状態でテストする↓

#### 正常 未実施 固有チェック　矯正区分：左の value は値があり、矯正：左の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：左の value に値がなく、矯正：左の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 以降のテストは、矯正区分：左の cancelReasonId の値がある状態で、矯正：左の  hasOrder = true かつ cancelReasonId の値がない状態でテストする↓

#### 正常 未実施 固有チェック　矯正区分：左の value は値があり、矯正：左の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：左の value に値がなく、矯正：左の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 以降のテストは、矯正区分：左の  hasOrder = true かつ cancelReasonId の値がない状態で、矯正：左の  hasOrder = false でテストする↓

#### 正常 未実施 固有チェック　矯正区分：左の value は値があり、矯正：左の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：左の value に値がなく、矯正：左の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 以降のテストは、矯正区分：左の  hasOrder = true かつ cancelReasonId の値がない状態で、矯正：左の  cancelReasonId の値がありでテストする↓

#### 正常 未実施 固有チェック　矯正区分：左の value は値があり、矯正：左の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：左の value に値がなく、矯正：左の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 以降のテストは、矯正区分：右と、矯正：右の  hasOrder = true かつ cancelReasonId の値がない状態でテストする↓

#### 正常 未実施 固有チェック　矯正区分：右と、矯正：右の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：右と、矯正：右の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：右の value は値があり、矯正：右の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] エラーメッセージ「矯正区分（右）が選択されていますが、矯正値（右）が入力されていません。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 固有チェック　矯正区分：右の value に値がなく、矯正：右の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] エラーメッセージ「矯正値（右）が入力されていますが、矯正区分（右）が選択されていません。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 以降のテストは、矯正区分：右の hasOrder = false で、矯正：右の  hasOrder = true かつ cancelReasonId の値がない状態でテストする↓

#### 正常 未実施 固有チェック　矯正区分：右の value は値があり、矯正：右の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：右の value に値がなく、矯正：右の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 以降のテストは、矯正区分：右の cancelReasonId の値がある状態で、矯正：右の  hasOrder = true かつ cancelReasonId の値がない状態でテストする↓

#### 正常 未実施 固有チェック　矯正区分：右の value は値があり、矯正：右の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：右の value に値がなく、矯正：右の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 以降のテストは、矯正区分：右の  hasOrder = true かつ cancelReasonId の値がない状態で、矯正：右の  hasOrder = false でテストする↓

#### 正常 未実施 固有チェック　矯正区分：右の value は値があり、矯正：右の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：右の value に値がなく、矯正：右の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 以降のテストは、矯正区分：右の  hasOrder = true かつ cancelReasonId の値がない状態で、矯正：右の  cancelReasonId の値がありでテストする↓

#### 正常 未実施 固有チェック　矯正区分：右の value は値があり、矯正：右の valueに値がない場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。

#### 正常 未実施 固有チェック　矯正区分：右の value に値がなく、矯正：右の valueに値がある場合
1. 視力コンポーネントが呼び出される
2. onRegisterPressed を true にする
* [ ] 固有チェックのエラーメッセージは表示されない。


### エラーメッセージ（基準値）


#### 正常 未実施 裸眼：左　基準値チェック　エラーレベル：異常
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージ「入力に誤りがあります。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 裸眼：左　基準値チェック　エラーレベル：警告
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージ「入力値を確認してください。」が表示される。
* [ ] アイコン：！
* [ ] 文字色：warning

#### 正常 未実施 裸眼：左　基準値チェック　該当なし
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージが表示されない。

#### 正常 未実施 裸眼：右　基準値チェック　エラーレベル：異常
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージ「入力に誤りがあります。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 裸眼：右　基準値チェック　エラーレベル：警告
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージ「入力値を確認してください。」が表示される。
* [ ] アイコン：！
* [ ] 文字色：warning

#### 正常 未実施 裸眼：右　基準値チェック　該当なし
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージが表示されない。

#### 正常 未実施 裸眼：両眼　基準値チェック　エラーレベル：異常
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージ「入力に誤りがあります。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 裸眼：両眼　基準値チェック　エラーレベル：警告
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージ「入力値を確認してください。」が表示される。
* [ ] アイコン：！
* [ ] 文字色：warning

#### 正常 未実施 裸眼：両眼　基準値チェック　該当なし
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージが表示されない。

#### 正常 未実施 矯正：左　基準値チェック　エラーレベル：異常
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージ「入力に誤りがあります。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 矯正：左　基準値チェック　エラーレベル：警告
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージ「入力値を確認してください。」が表示される。
* [ ] アイコン：！
* [ ] 文字色：warning

#### 正常 未実施 矯正：左　基準値チェック　該当なし
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージが表示されない。

#### 正常 未実施 矯正：右　基準値チェック　エラーレベル：異常
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージ「入力に誤りがあります。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 矯正：右　基準値チェック　エラーレベル：警告
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージ「入力値を確認してください。」が表示される。
* [ ] アイコン：！
* [ ] 文字色：warning

#### 正常 未実施 矯正：右　基準値チェック　該当なし
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージが表示されない。

#### 正常 未実施 矯正：両眼　基準値チェック　エラーレベル：異常
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージ「入力に誤りがあります。」が表示される。
* [ ] アイコン：×
* [ ] 文字色：error

#### 正常 未実施 矯正：両眼　基準値チェック　エラーレベル：警告
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージ「入力値を確認してください。」が表示される。
* [ ] アイコン：！
* [ ] 文字色：warning

#### 正常 未実施 矯正：両眼　基準値チェック　該当なし
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスに基準値チェックに該当する値を入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージが表示されない。


### エラーメッセージ（半角数値）


#### 正常 未実施 裸眼：左　数値チェック「abc」
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスに「abc」と入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージ「裸眼は半角数字で入力してください。」が表示される
* [ ] アイコン：×
* [ ] 文字色：error
#### 正常 未実施 裸眼：左　数値チェック「1.0」
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスに「1.0」と入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージが表示されない。
#### 正常 未実施 裸眼：右　数値チェック「abc」
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスに「abc」と入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージ「裸眼は半角数字で入力してください。」が表示される
* [ ] アイコン：×
* [ ] 文字色：error
#### 正常 未実施 裸眼：右　数値チェック「1.0」
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスに「1.0」と入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージが表示されない。
#### 正常 未実施 裸眼：両眼　数値チェック「abc」
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスに「abc」と入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージ「裸眼は半角数字で入力してください。」が表示される
* [ ] アイコン：×
* [ ] 文字色：error
#### 正常 未実施 裸眼：両眼　数値チェック「1.0」
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスに「1.0」と入力する
3. onRegisterPressed を true にする
* [ ] 裸眼にエラーメッセージが表示されない。
#### 正常 未実施 矯正：左　数値チェック「abc」
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスに「abc」と入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージ「矯正は半角数字で入力してください。」が表示される
* [ ] アイコン：×
* [ ] 文字色：error
#### 正常 未実施 矯正：左　数値チェック「1.0」
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスに「1.0」と入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージが表示されない。
#### 正常 未実施 矯正：右　数値チェック「abc」
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスに「abc」と入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージ「矯正は半角数字で入力してください。」が表示される
* [ ] アイコン：×
* [ ] 文字色：error
#### 正常 未実施 矯正：右　数値チェック「1.0」
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスに「1.0」と入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージが表示されない。
#### 正常 未実施 矯正：両眼　数値チェック「abc」
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスに「abc」と入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージ「矯正は半角数字で入力してください。」が表示される
* [ ] アイコン：×
* [ ] 文字色：error
#### 正常 未実施 矯正：両眼　数値チェック「1.0」
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスに「1.0」と入力する
3. onRegisterPressed を true にする
* [ ] 矯正にエラーメッセージが表示されない。


## value の値が変更された時
### エラーメッセージの非表示チェック（必須チェック）


#### 正常 未実施 裸眼：左のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスを空欄から「1.0」に変更する
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。

#### 正常 未実施 　
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスを空欄から「1.0」に変更する
* [ ] 全てのコントロールの色が error から　デフォルト色 に変更される。

#### 正常 未実施 裸眼：右のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスを空欄から「1.0」に変更する
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。

#### 正常 未実施 裸眼：両眼のデータを変更しエラーを解消する　
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスを空欄から「1.0」に変更する
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。

#### 正常 未実施 矯正区分：左の項目を選択し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 矯正区分（左）の選択肢ボタンを全て未選択の状態から、いずれかをタップする
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。

#### 正常 未実施 矯正区分：右の項目を選択し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 矯正区分（右）の選択肢ボタンを全て未選択の状態から、いずれかをタップする
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。

#### 正常 未実施 矯正：左のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスを空欄から「1.0」に変更する
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。

#### 正常 未実施 矯正：右のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスを空欄から「1.0」に変更する
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。

#### 正常 未実施 矯正：両眼のデータを変更しエラーを解消する　
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスを空欄から「1.0」に変更する
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。

#### 正常 未実施 特記：左の項目を選択し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 特記（左）の選択肢ボタンを全て未選択の状態から、いずれかをタップする
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。

#### 正常 未実施 特記：右の項目を選択し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 特記（右）の選択肢ボタンを全て未選択の状態から、いずれかをタップする
* [ ] エラーメッセージ「視力は入力必須項目です。」が非表示になる。


### エラーメッセージの非表示チェック（固有チェック）


#### 正常 未実施 矯正：左のデータを変更し、エラーを解消する　矯正：左が未測定、かつ、矯正区分：左が選択済み
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスを空欄から「1.0」に変更する
* [ ] エラーメッセージ「矯正区分（左）が選択されていますが、矯正値（左）が入力されていません。」が非表示になる。

#### 正常 未実施 矯正：左のデータを変更し、エラーを解消する　矯正：左が測定済み、かつ、矯正区分：左が未選択
1. 視力コンポーネントが呼び出される
2. 矯正区分（左）の選択肢ボタンを全て未選択の状態からいずれかをタップする
* [ ] エラーメッセージ「矯正値（左）が入力されていますが、矯正区分（左）が選択されていません。」が非表示になる。

#### 正常 未実施 矯正：右のデータを変更し、エラーを解消する　矯正：右が未測定、かつ、矯正区分：右が選択済み
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスを空欄から「1.0」に変更する
* [ ] エラーメッセージ「矯正区分（右）が選択されていますが、矯正値（右）が入力されていません。」が非表示になる。

#### 正常 未実施 矯正：右のデータを変更し、エラーを解消する　矯正：右が測定済み、かつ、矯正区分：右が未選択
1. 視力コンポーネントが呼び出される
2. 矯正区分（右）の選択肢ボタンを全て未選択の状態からいずれかをタップする
* [ ] エラーメッセージ「矯正値（右）が入力されていますが、矯正区分（右）が選択されていません。」が非表示になる。

### エラーメッセージの非表示チェック（基準値チェック）



#### 正常 未実施 裸眼：左のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 裸眼（左）の入力値テキストボックスに基準値チェックを解消する値を入力する
* [ ] エラーメッセージが非表示になる。

#### 正常 未実施 裸眼：右のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 裸眼（右）の入力値テキストボックスに基準値チェックを解消する値を入力する
* [ ] エラーメッセージが非表示になる。

#### 正常 未実施 裸眼：両眼のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 裸眼（両眼）の入力値テキストボックスに基準値チェックを解消する値を入力する
* [ ] エラーメッセージが非表示になる。

#### 正常 未実施 矯正：左のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 矯正（左）の入力値テキストボックスに基準値チェックを解消する値を入力する
* [ ] エラーメッセージが非表示になる。

#### 正常 未実施 矯正：右のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 矯正（右）の入力値テキストボックスに基準値チェックを解消する値を入力する
* [ ] エラーメッセージが非表示になる。

#### 正常 未実施 矯正：両眼のデータを変更し、エラーを解消する
1. 視力コンポーネントが呼び出される
2. 矯正（両眼）の入力値テキストボックスに基準値チェックを解消する値を入力する
* [ ] エラーメッセージが非表示になる。

