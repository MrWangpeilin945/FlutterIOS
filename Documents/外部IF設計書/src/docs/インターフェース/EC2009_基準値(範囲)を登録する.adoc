== EC2009_基準値(範囲)を登録する

=== 概要

* 外部連携で使用する
* 引数の検査基準値範囲をDBに登録する

=== 処理フロー

[mermaid]
----
flowchart TD
    A[UseCase] --> ERROR{ERROR検証}
    ERROR -- エラーあり --> C(ErrorObject生成、追加)
    ERROR -- エラー無 --> WARNING{WARNING検証}    
    WARNING -- ワーニングあり --> DROP(WARNINGレコードDrop)
    DROP(WARNINGレコードDrop) --> DROP_REC(ErrorObject生成、追加)
    DROP_REC --> D[[Repository]]
    WARNING -- ワーニングなし --> D
    C --> E(返却)
    D --> E
    E --> UseCaseEND(END)

    F[[Repository]] --> G[BEGIN]
    G --> H["Upsert(一括):[DB]exam_normal_value_range(検査基準値範囲)"]
    H --> K[Commit]
    H -.Exception.-> ROLLBACK[ROLLBACK]
    K --> RepositoryEND[[END]]
    ROLLBACK .-> RepositoryEND
----

=== 非機能

* 1万レコードの登録を10秒以内に完了する
* DB登録に失敗した場合、全てロールバックする

=== UseCase

Wellship.WebAPI

[source]
----
StoreExamNormalValueRangeAsync
----

==== 引数

[cols="1,1,1,2" options="header"]
|===
|名称|必須|型|説明

|ExamNormalValueRanges
|Yes
|List<ExamNormalValueRange>
|登録する基準値(範囲)のリスト

|===

==== 戻り値

[source]
----
list<ErrorObject>
----

===== 正常登録

[source]
----
list.Count() == 0
----

===== 異常有り

[source]
----
list.Count() <> 0
----

==== クラス

===== ExamNormalValueRange

[cols="1,1,1,2" options="header"]
|===
|名称|必須|型|説明

|Name
|Yes
|string
|名称

|ThresholdCd
|Yes
|string
|基準値パターンCD

|ExamItemDetailCd
|Yes
|string
|検査項目明細CD

|MaxAge
|Yes
|string
|対象年齢上限 形式：yyyMMdd(年齢月齢日齢)

|MinAge
|Yes
|string
|対象年齢下限 形式：yyyMMdd(年齢月齢日齢)

|TargetSex
|Yes
|enum<Wellship.Core\Enums\TargetSex>
|対象性別

|MaxValue
|Yes
|decimal
|値上限

|MinValue
|Yes
|decimal
|値下限

|ErrorLevel
|Yes
|enum<Wellship.Core\Enums\InputErrorLevel>
|エラーレベル

|InputNote
|Yes
|No
|エラーオブジェクトに登録する入力項目Noなど

|===

==== ErrorObject

[cols="1,1,1,2" options="header"]
|===
|名称|必須|型|説明

|Code
|Yes
|string
|エラーコード

|Message
|Yes
|string
|エラーメッセージ

|InputNote
|No
|string
|入力項目Noなど

|===

==== 列挙型

* None

==== ERROR検証

* None

==== WARNING検証

* 必須項目(ExamNormalValueRange.Name)が空値
** `ExamNormalValueRange.Name` が `string.IsNullOrWhiteSpace` に該当する 
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10004`
**** Message `必須項目が不足しています。Name`
**** InputNote `ExamNormalValueRange.InputNote`

* 必須項目(ExamNormalValueRange.ThresholdCd)が空値
** `ExamNormalValueRange.ThresholdCd` が `string.IsNullOrWhiteSpace` に該当する 
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10004`
**** Message `必須項目が不足しています。ThresholdCd`
**** InputNote `ExamNormalValueRange.InputNote`

* 必須項目(ExamNormalValueRange.ExamItemDetailCd)が空値
** `ExamNormalValueRange.ExamItemDetailCd` が `string.IsNullOrWhiteSpace` に該当する 
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10004`
**** Message `必須項目が不足しています。ExamItemDetailCd`
**** InputNote `ExamNormalValueRange.InputNote`

* 必須項目(ExamNormalValueRange.MaxAge)が空値
** `ExamNormalValueRange.MaxAge` が `string.IsNullOrWhiteSpace` に該当する 
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10004`
**** Message `必須項目が不足しています。MaxAge`
**** InputNote `ExamNormalValueRange.InputNote`

* 必須項目(ExamNormalValueRange.MinAge)が空値
** `ExamNormalValueRange.MinAge` が `string.IsNullOrWhiteSpace` に該当する 
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10004`
**** Message `必須項目が不足しています。MinAge`
**** InputNote `ExamNormalValueRange.InputNote`

* 項目(ExamNormalValueRange.MaxAge)制限：形式
** `ExamNormalValueRange.MaxAge` が `^[0-9]{3}(0[0-9]|1[01])([012][0-9]|30)$` に一致しない
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10006`
**** Message `値の形式が無効です。MaxAge:[ExamNormalValueRange.MaxAge]`
**** InputNote `ExamNormalValueRange.InputNote`
** 補足：正常範囲　`0000000` `9991130`

* 項目(ExamNormalValueRange.MinAge)制限：形式
** `ExamNormalValueRange.MinAge` が `^[0-9]{3}(0[0-9]|1[01])([012][0-9]|30)$` に一致しない
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10006`
**** Message `値の形式が無効です。MinAge:[ExamNormalValueRange.MinAge]`
**** InputNote `ExamNormalValueRange.InputNote`
** 補足：正常範囲　`0000000` `9991130`

* 項目制限：年齢大小
** `ExamNormalValueRange.MinAge` < `ExamNormalValueRange.MaxAge` に一致しない
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10007`
**** Message `値の範囲が無効です。MinAge:[ExamNormalValueRange.MinAge]/MaxAge:[ExamNormalValueRange.MaxAge]
**** InputNote `ExamNormalValueRange.InputNote`
** 補足：範囲は　Min以上 Max未満となるため、`=(一致)` はエラーとする

* 項目制限：基準値大小
** `ExamNormalValueRange.MinValue` < `ExamNormalValueRange.MaxValue` に一致しない
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10007`
**** Message `値の範囲が無効です。MinValue:[ExamNormalValueRange.MinValue]/MaxValue:[ExamNormalValueRange.MaxValue]
**** InputNote `ExamNormalValueRange.InputNote`
** 補足：範囲は　Min以上 Max未満となるため、`=(一致)` はエラーとする

* 基準値パターンが取得できない
** `[DB]thresholds(基準値パターン).threshold_code(基準値パターンコード)` == `ExamNormalValueRange.ThresholdCd` が存在しない
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10001`
**** Message `指定されたThresholdCdがシステム上に存在しません。Code:[ExamNormalValueRange.ThresholdCd]`
**** InputNote `ExamNormalValueRange.InputNote`

* 検査項目明細IDが取得できない
** `[DB]external_exam_item_details(外部検査項目明細).external_exam_item_detail_code(外部コード検査項目明細CD)` == `ExamNormalValueRange.ExamItemDetailCd` が存在しない
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10001`
**** Message `指定されたExamItemDetailCdがシステム上に存在しません。Code:[ExamNormalValueRange.ExamItemDetailCd]`
**** InputNote `ExamNormalValueRange.InputNote`

* PKが重複するレコードが存在する
** ExamNormalValueRangesを以下の条件でGroupByし、複数レコードが存在する
**** `[DB]thresholds.threshold_code` == `ExamNormalValueRange.ThresholdCd` で検索した `[DB]thresholds.threshold_id`
**** `[DB]external_exam_item_details.external_exam_item_detail_code` == `ExamNormalValueRange.ExamItemDetailCd` で検索した `[DB]external_exam_item_details.exam_item_detail_id`
**** `ExamNormalValueRange.TargetSex`
**** `ExamNormalValueRange.MaxAge` ※左0埋め7桁にそろえる
**** `ExamNormalValueRange.MaxValue`
*** ExamNormalValueRangeの単位で該当を取り除く
*** ErrorObject
**** Code `10003`
**** Message `キー項目が重複しています。Code:[ExamNormalValueRange.ThresholdCd]/[ExamNormalValueRange.ExamItemDetailCd]/[ExamNormalValueRange.TargetSex]/[ExamNormalValueRange.MaxAge]/[ExamNormalValueRange.MaxValue]`
**** InputNote `ExamNormalValueRange.InputNote`

==== 処理(Repository)

===== Table

* [DB]exam_normal_value_range(検査基準値範囲)
** 洗い替え

====== Delete

* 削除条件
** `[DB]exam_normal_value_range.threshold_id(基準値パターンID)` == `[DB]thresholds.threshold_code` == `ExamNormalValueRange.ThresholdCd` で検索した `[DB]thresholds.threshold_id`
And `[DB]exam_normal_value_range.exam_item_detail_id(検査項目明細ID)` == `[DB]external_exam_item_details.external_exam_item_detail_code` == `ExamNormalValueRange.ExamItemDetailCd` で検索した `[DB]external_exam_item_details.exam_item_detail_id`
*** 削除対象レコードが存在しない場合でも、エラーとしない

====== Insert

[cols="1,2,2" options="header"]
|===
|列|登録値|処理・変換

|range_id
|
|DB:DEFAULT

|name
|ExamNormalValueRange.Name
|

|threshold_id
|`[DB]thresholds.threshold_id`
|`[DB]thresholds.threshold_code` == `ExamNormalValueRange.ThresholdCd` で検索し、`threshold_id`を登録する

|exam_item_detail_id
|`[DB]external_exam_item_details.exam_item_detail_id`
|`[DB]external_exam_item_details.external_exam_item_detail_code` == `ExamNormalValueRange.ExamItemDetailCd` で検索し、`exam_item_detail_id`を登録する

|max_age
|ExamNormalValueRange.MaxAge
|左0埋め7桁

|min_age
|ExamNormalValueRange.MinAge
|左0埋め7桁

|target_sex
|ExamNormalValueRange.TargetSex
|int型にパースする

|max_value
|ExamNormalValueRange.MaxValue
|

|min_value
|ExamNormalValueRange.MinValue
|

|error_level
|ExamNormalValueRange.ErrorLevel
|int型にパースする

|created_at
|system.datetime.now()
|

|created_by
|`'ExternalConnection'`
|

|===

=== レビュー履歴

WELLSHIP_DEV-922 [外部IF_設計]EC2009_基準値(範囲)を登録する レビュー・リワーク