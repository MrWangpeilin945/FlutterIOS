== EC2008_団体を登録する

=== 概要

* 外部連携で使用する
* 引数の団体をDBに登録する

=== 処理フロー

[mermaid]
----
flowchart TD
    A[UseCase] --> B{検証}
    B -- エラーあり --> C(ErrorObject生成、追加)
    B -- エラー無 --> D[[Repository]]
    C --> E(返却)
    D --> E
    E --> UseCaseEND(END)

    F[[Repository]] --> G[BEGIN]
    G --> H["Upsert(一括):[DB]organizations(団体)"]
    H --> K[Commit]
    H -.Exception.-> ROOLBACK[ROOLBACK]
    K --> RepositoryEND[[END]]
    ROOLBACK .-> RepositoryEND
----

=== 非機能

* 1万レコードの登録を10秒以内に完了する
* 検証・DB登録に失敗した場合、全てロールバックする

=== UseCase

[source]
----
StoreorganizationsAsync
----

==== 引数

[cols="1,1,1,2" options="header"]
|===
|名称|必須|型|説明

|organizations
|Yes
|List<Organization>
|登録する受診者のリスト

|===

==== 戻り値

[source]
----
list<ErrorObject>
----

===== 正常登録

[source]
----
list.Count() == 0
----

===== 異常有り

[source]
----
list.Count()  <> 0
----

==== クラス

===== Organization

[cols="1,1,1,2" options="header"]
|===
|名称|必須|型|説明

|Code
|Yes
|string
|班コード

|Name
|Yes
|string
|班名

|===

==== 列挙型

* None

==== 検証

* None

==== 処理(Repository)

===== Insert

* 条件
** `[DB]organizations(団体).organization_code(団体コード) == Organization.Code` が存在しない

===== Table

* [DB]organizations(団体)

[cols="1,2,2" options="header"]
|===
|列|登録値|処理・変換

|team_id
|max([DB]organizations(団体).organization_id(団体ID)) + 1
|

|organization_code
|Organization.Code
|

|name
|Organization.Name
|

|order_number
|max([DB]organizations(団体).order_number(表示順)) + 1
|

|created_at
|system.datetime.now()
|

|created_by
|`'ExternalConnection'`
|

|===

===== Update

* 条件
** `[DB]organizations(団体).organization_code(団体コード) == Organization.Code` が存在する

===== Table

* [DB]organizations(団体)

[cols="1,2,2" options="header"]
|===
|列|登録値|処理・変換

|organization_id
|更新対象外
|

|organization_code
|更新対象外
|

|name
|Organization.Name
|

|order_number
|更新対象外
|

|created_at
|system.datetime.now()
|

|created_by
|`'ExternalConnection'`
|

|===